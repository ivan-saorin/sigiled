# Task B3b — research and model views through the browser driver

Execution note: read driver instructions only from `work/sigil-skill-redacted.md`; never print the original credential-bearing skill or private session files. `work/sigil_client.py` handles authentication privately.

This task follows accepted A2/A3, B1/B2 and B3a. Parent supplies the then-current Sigil and SDE interfaces at dispatch. Own Sigil's typed SDE/ADHD/Genie adapters and Research/Models browser views, their tests and docs. You are not alone in the codebase; preserve other work. No subagents, deployments, paid inference or unsolicited model downloads/loads. Use the active Sigil workspace through `work/sigil_client.py`; do not open, close or recycle it. Report to `work/research-adapter-report.md` and commit via the workspace API.

The planned attribution boundary is recorded in work/research-identity-note.md; the accepted B3a report will supply its exact implementation. Sigil's durable association is authoritative for browser actor/project attribution. Do not convert caller-supplied SDE metadata into verified identity or expose private deduplication keys in UI responses.

B2 integration notes (parent confirms accepted head at dispatch): use shared `/ui/research`, `/ui/models` and `/ui/projects/{project}/research` routes. Assets are in `sigiledd/src/browser/assets/`; the actual-source browser harness is `sigiledd/tests/browser-ui.cjs`. `window.Sigil` exposes `el`, `request`, `navigate`, `showAuth`, `checkSession`. Extend the narrow body-size method/path allowlists in `browser/dashboard.rs`, preserve its draft/focus/slow-response handling and separate-tab sign-in recovery, and never automatically replay a research mutation after login. B2 work-item APIs and its durable store are separate from derived attention; do not create duplicate task/audit storage.

## Integration contract

- Resolve reserved SDE, ADHD and Genie IDs from the accepted registry, with exact supported endpoint/operation allowlists. No arbitrary URL proxy, path traversal, redirects or forwarding browser Cookie/Host headers. Pass the actual server-held browser originator access token on each downstream call. Use bounded connection/response/body timeouts and bytes; safe errors omit credentials and untrusted raw upstream HTML/logs. Return observation timestamps and typed unavailable/stale states; one unavailable engine must not blank the dashboard.
- Research list/detail exposes the actual SDE status, stages, caller deadlines, computed-by provenance, corpus, dossier, decision and handoff availability. Render model/source text as text or sanitized Markdown with safe links. Link ADHD details from the recorded `adhd_run_id`; do not invent an ADHD list endpoint. Show degraded outcomes and warnings independently from terminal success.
- Provide explicit project-associated research creation with a visible problem/context/options form, bounded validated options, operation ID/idempotency support from B3a, CSRF and origin checks. Starting research can call paid external providers; only the user's deliberate Start action triggers it. Browser use without an attached caller agent sends `options.delegate=[]` so stages are not parked waiting on a nonexistent agent. Explain the effect in ordinary UI language. An explicit attached-caller mode is allowed only if it has a real route for fulfilling the contract.
- Resume is available only for failed/resumable runs and uses the newly validated current originator token. Do not automatically repeat ambiguous POSTs. Awaiting-caller runs show the actual deadline and exact stage payload in an inspectable surface; optional submitted results follow the existing SDE schema, not invented browser synthesis. Completed no-decision runs expose the supported convergence path honestly. Do not rewrite stage failures as completed results.
- Keep project associations durable and attributable. Legacy/global runs must remain visibly unassociated; don't guess from text. If association belongs in the browser driver, persist it atomically with operation recovery and use B3a's idempotency/query contract to reconcile an ambiguous network response. Handle renamed/missing project references without silently reassigning history.
- Scope operation recovery to the stable verified actor and project, not only the temporary browser-login ID. Explicit sign-in recovery must be able to find the same accepted operation without creating a second paid run; a different actor must not take over its operation identity.
- Before enabling new retry-sensitive research mutations, check B3a's live contract/capability response with bounded freshness. An older running SDE may ignore new fields or idempotency keys; missing/unknown support means “Research service update required,” not an unsafe automatic fallback. Preserve compatible read views where possible. Do not infer live support solely from the new source manifest or repository revision. Test an older exact-contract fixture and verify no research POST is issued when support is unconfirmed.
- Handoff preview shows exactly the dossier/cover files and target project before action. The approved action uses Sigil's existing human session protocol: inspect merge debt while selecting/opening the owned workspace, then read that workspace's Git log before any write. Validate bundle paths and sizes, write the files without overwriting unrelated changes, commit to its session branch, then show the IDE. Preserve work on every failed step. Do not mark accepted into master or indexed in memory until those transitions are confirmed. D3 owns accepted-artifact enrollment; report a durable operation state it can consume, keyed by run and accepted commit.
- Serialize the handoff write/commit phase with the existing A1 session lifecycle lock and revalidate the recorded generation. C1's accepted final checkpoint stops its owned editor and rejects observed concurrent changes; it cannot supervise an unrelated HTTP file writer. A platform handoff must not start or continue writes after finish/close owns that session. Preserve the established session-before-project lock order and avoid reacquiring the same lock through a nested public handler.
- Account explicitly for the workspace `/git/commit` contract's `add -A`: a handoff preview showing only dossier files must not silently commit unrelated pre-existing staged or unstaged edits. Either use a proven isolated commit preserving the user's index/files, or return a recoverable workspace-changes precondition before writing so the user can deliberately checkpoint and retry. Test both staged and unstaged unrelated content, plus concurrent editor changes, rather than assuming lifecycle serialization also stops arbitrary file writers.
- Genie views show `/info`, `/status` and bounded `/usage` truthfully: slot/provider/model, local lock holder, download/load/error state, recent observed requests and optional token counts. The inspected ledger can lose records on write failure and has no per-project attribution. Label this a recent observation window, not full billing or project totals; unknown tokens stay unknown. Do not add SDE/ADHD usage on top of Genie totals. Don't derive money without explicit current price data. Any model bind/load/unload/download control must be a deliberate authenticated action with existing resource-owner rules; it may be deferred as read-only if doing it would require a separate product decision.
- Add research attention items to the dashboard using the latest actual run state; failures or caller waits clear when resolved. Don't duplicate the B2 work-item store or fork its audit/authorization rules. Preserve all existing machine APIs.

## Verification and delivery

Use fake exact-contract services and the real browser assets with the local Edge/Playwright harness prepared for B2. Test caller-token pass-through/redaction, cross-origin/CSRF rejection, path/redirect/body bounds, unauthorized and expired auth, stale service isolation, project association, double click/idempotency/ambiguous response, caller-delegation choices, complete versus degraded, safe Markdown, resume preserving stages, handoff path validation and preservation on write/commit failure, unknown versus zero usage. No live inference. Read-only live checks can be run when available but do not substitute fixtures for end-to-end claims.

Verify keyboard/mobile layouts and visible loading/error/empty states. Store representative screenshots, meaningful test logs and exact commit IDs. Document any remaining live prerequisites and integration work, then request parent review of the actual diff.

## Accepted IDE consumer reference

C2 is independently accepted at 18ad999df09dbc59296a1badd2d2f0cdbbef56e8. Read work/ide-gateway-consumer-note.md for allocation, exact browser generation strings, file targets, readiness/recovery and source-preserving handoff boundaries. Parent will supply the current clean Sigil task BASE after controller documentation commits. Status observations from the synthetic C2 browser fixture are not live transport proof.

Version fidelity: the B3a candidate contract uses numeric uint64 revisions on its machine interface. Once that contract is independently accepted, preserve the full revision when projecting to browser JSON and when forwarding an expected revision. Use the established canonical decimal-string browser pattern if the backend permits full uint64, rather than JavaScript Number coercion; test a value above the safe integer range. This is separate from unchanged machine numeric JSON. Parent will refresh exact accepted types after review.

## B3a accepted interface and active implementation context

Read work/research-engine-consumer-note.md for the accepted exact SDE API/capability contract. Independently reviewed source and verified master: af836b3f9ebef74bfd6624e382fc80ab371251a9. The live app remains older; no upgrade occurred. Work/research-engine-report.md and review contain evidence; no need to read all embedded logs for implementation. Work/candidate-sde-core-run-types.rs preserves the current core record/options definitions for read-only reference; verify against accepted report/package if needed, never treat local copies as editable app checkout. SDE workspace de4f0115 is closed; do not reuse its former endpoint/token or mutate SDE in this task. Any required backend gap goes to parent with a concrete explanation.

Sigil session 877a5495 remains open on session/877a5495. Parent supplies exact task BASE after publishing this brief. Do not open/recycle/close. Read work/integration-contracts.md and work/ide-gateway-consumer-note.md for accepted B1/B2/C2 boundaries. Driver instructions only work/sigil-skill-redacted.md. Keep all authoritative source edits/builds/tests/commits remote via helper project=sigiled; no local application checkout. Add operational journal before committing and leave clean/pushed/open for review.

SDE recovery uses deliberate POST /runs with the same privately stored UUIDv4 operation key and canonical request; it never automatically resumes. A replay is recoverable even when catalog refresh is unavailable. New start/retry-sensitive mutation readiness must require the exact sde-runs-v1 capability AND durable, nondegraded persistence for the browser durable-operation guarantee. Memory-only or repair/restart status needs clear guidance and no unsafe accepted-durable claim. Show completed stages separately from unfinished work that may repeat on an explicit resume.

Browser UI follows applied frontend-design guidance and work/browser-design.md. Own only this task's Sigil source/tests/docs plus local report/evidence and representative outputs screenshots; you are not alone, preserve all existing edits. Controller owns orchestration docs. Use targeted RED/GREEN and one relevant regression, retaining actual .exit plus outer /exec timeout/truncation receipts. Earlier Sigil full regression199/2 and slow lifecycle diagnostic remain unresolved (work/timing-investigation-note.md), so report exact final results without claiming focused checks establish full-suite green. Do not change the shared process supervisor or weaken timing budgets in this task; report a concrete blocker if it prevents progress.
