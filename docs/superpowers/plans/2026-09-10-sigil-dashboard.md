# Task B2 — the working Sigil dashboard

Read this first. Parent supplies the accepted A2/A3/B1 API shapes at dispatch. Use `work/browser-design.md` for the visual plan and the frontend-design skill guidance. Implement a working application, not a static mock or demo with invented live data.

## B1 integration notes (accepted edb9ff8b9151c2e70fe583c0d653782fbcbbd442)

Carry forward one non-blocking B1 review minor: fix the corrupted callback placeholders in remote `docs/browser-auth.md:12` to plain ASCII `code=CODE&state=STATE`. Runtime behavior was accepted; do not reopen the authentication design for this documentation edit.

The B1 report is `work/browser-auth-report.md`; remote contract is `docs/browser-auth.md`. Root paths are `/browser/login?return_to=...`, `/browser/callback`, `/browser/session`, `/browser/logout`, `/browser/api/overview` and `/browser/api/projects/{project}`. Browser cookies never authenticate the existing bare or `/sigiled` machine routes.

`browser::BrowserContext` implements `FromRequestParts<AppState>` and carries the verified Actor, issuer, subject, separate display name and crate-private server-held access token. It validates exact Origin and `X-Sigil-CSRF` for every method except GET/HEAD. Session inspection returns `actor`, `identity`, `csrf_token`, idle/absolute expiry, feature flags and capability/role metadata. Use these values, not a second cookie or identity mechanism. The stable browser driver is `human:` plus base64url SHA-256 over the JSON issuer/subject tuple.

B1 currently disallows bodies on its own routes and imposes URI/header/time limits. Add route-specific bounded JSON bodies for the new POST/PATCH handlers while preserving the existing bodyless login/logout/read contract; do not remove the whole browser boundary to admit forms. Extend feature projections only for actual implemented actions. Serve the dashboard/assets only on its explicitly configured browser origin, preserve machine-origin behavior, and keep the later memory origin separately selectable. Coordinate exact public additions in the report for C2/D2.

## Execution and ownership

Own embedded browser assets/shared navigation, narrow browser API adapters needed for the dashboard, explicit work items, UI tests and docs in `sigiled`. Preserve the reviewed foundation and authentication; you are not alone in the codebase. No subagents, real project/app/job/model mutations or deployment. Use the parent's existing workspace session via API; normal `/git/commit` stages all and pushes. Keep local browser fixtures and screenshots under work or outputs only; no credentials in assets or fixtures. Report `work/dashboard-report.md` with routes, commits, test evidence/screenshots and outstanding live prerequisites.

## Required experience

- Serve the root Sigil browser view plus self-hosted assets with B1 sign-in/session handling. Actual live aggregate APIs drive every project row and state. New registered projects appear automatically without frontend edits or a control-plane rebuild for their name. No hard-coded project list. URLs are navigable/back-button compatible and restore selected project/tab/filter.
- Shared shell: Overview, Projects, Research, Memory, Models; signed-in operator and sign-out. Lead with Needs attention and active work, then a searchable/filterable/sortable project table. Show source/observation time and degraded/stale/unavailable states; never silently show zeros or healthy when fetch fails. Poll boundedly, stop while hidden when appropriate, preserve focus/selection, and do not touch workspace activity.
- Project view: Overview, Workspace, Research, Jobs, App, Memory, Activity, Pending tabs. Tabs reflect available typed capabilities; unsupported operations get a clear setup reason. Workspace shows real sessions, actors, branch, lifecycle, generation and safe readiness. Repository/deployed revisions are distinct. Jobs show actual definitions/latest/history; never expose job commands or secret mappings in the general view. App status preserves not-deployed/building/failed/unknown distinctions. Activity is paginated. Research/Models consume later B3 adapters; no fake research results or inference calls here.
- New project form invokes the existing authenticated project-creation policy through a narrow CSRF-protected B1 adapter. Show validation, required approval and partial provisioning honestly. UI retry must not duplicate registration. Every browser mutation uses B1's actual actor/context. Never forward raw workspace tokens from existing handlers to the browser.
- Enforce creation retry/concurrency behavior server-side, including two tabs and an ambiguous response; disabling a button alone is insufficient. Inspect the existing create/adopt/key-registration path and keep repeated attempts from rotating an incumbent project's deploy key or claiming rollback of an already-created remote repository. Reuse core project locking and return the existing operation/project state where safe. Do not invent a new repository provisioning engine.
- Implement explicit project work items, distinct from derived system attention. Fields: opaque ID, project, title, optional description, state open/blocked/done, owner, source link, created/updated timestamps, revision, authenticated creator/editor and audit history. Create/list/detail/update with expected-revision conflict handling, bounded input/pagination, stable source links, and persistence across restart. No automatic inference of tasks from arbitrary prose and no marking system attention done manually. A persistence failure must not be acknowledged as saved; use a small coherent durable store/transaction, not a lossy mutation followed by a swallowed write error. Source links must be safe validated URLs/relative app paths. Don't add a scheduler or unrelated project-management features.
- Integrate the reviewed IDE launch/checkpoint/finish controls when their APIs are ready; until then render actual setup status with unavailable controls, and record remaining integration as incomplete rather than claiming the IDE is delivered. Same rule for memory curation and SDE actions. A browser disconnect never closes or merges a workspace.
- Browser rendering uses text nodes or a sanitizing renderer, with no HTML interpolation of project descriptions, memory text, model outputs or errors. CSP and no-store behavior align with B1. Do not inject secrets into boot data. Keyboard focus, semantic headings/buttons/tables, labels, empty/loading/error states, narrow/mobile layout and reduced motion are required.
- Polling and delayed navigation responses must preserve typed form drafts, focus and the currently selected project. Ignore/abort stale responses rather than letting a slow previous page replace the new one. Use readable status/action labels; raw field names and principal hashes belong only in an optional diagnostic detail.
- If authentication expires during an edit, retain the unsaved draft in the current page and show a clear sign-in/retry path; do not replace the entire editing surface and erase the user's text on a background 401. Credentials remain server-held. A successful new login never automatically replays a previously attempted mutation.

## Verification

Use actual in-process API handlers with controlled fixtures for persistence/authorization/CAS and no live mutations. Frontend checks should exercise login-required/session-expired, project list/search/filter, navigation/back, new project validation, stale/error responses, task create/update/conflict, pagination and malicious text. Use a browser at desktop and mobile widths and inspect screenshots if tooling is available. Browser fixtures must be clearly identified as fixtures; production uses APIs only. A static screenshot alone is not functional acceptance.

Available local browser tooling was verified: Node is installed; bundled Playwright is at `C:/Users/ivans/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.js`. Load it via Node createRequire from that bundle's node/package.json. Its downloaded Chromium is absent, but installed Edge exists at `C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe`; use headless Playwright with that executable or channel `msedge`. No additional browser/package download is necessary. Copy authored assets from the remote workspace to local work/ for tests against a loopback fixture server; never expose real credentials in that server. Remote remains authoritative for committed assets.

Parent executed a headless smoke on 2026-09-10: Edge 152.0.4191.66 launched and a semantic-button keyboard-focus assertion passed. This only verifies tooling; no product UI test has run yet. The runtime can be loaded from a CommonJS script with `createRequire('C:/Users/ivans/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/package.json')('playwright')`.

Prefer a small shared asset bundle consistent with the existing Rust application; avoid a gratuitous second platform/build chain. Record exact browser/backend test commands and results. Run relevant regressions once after final fixes, update contract/log-operativo and commit source. Parent will independently review behavior and visual quality; live domain/OIDC testing remains a release step.
