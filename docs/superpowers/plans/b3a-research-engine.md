# Task B3a — SDE persistence and browser-compatible run contracts

Execution note: read driver instructions only from `work/sigil-skill-redacted.md`; never print the original credential-bearing skill or private session files. `work/sigil_client.py` handles authentication privately.

Implement in SDE only, using a fresh isolated workspace session supplied by the parent. Read the actual current Git log and any merge debt first. You are not alone in the codebase; preserve other changes. No subagents, no deployment, no paid inference, no model lifecycle calls, no automatic workspace handoff. Use `work/sigil_client.py` through the remote workspace API. Keep scratch/build logs under ignored target, cap Cargo builds at two jobs, commit via `/git/commit`, report to `work/research-engine-report.md`.

## Purpose and inspected baseline

SDE remains the research coordinator. It already calls Genie, paper, search, folio and ADHD, persists stage artifacts, accepts caller-computed stages, resumes failures, and emits the handoff bundle. The new UI must not reimplement that pipeline. Baseline inspected at d06bfaaff8ff72085bb52ddd9a80c86447f3a5ba; deployed app was older. Reinspect narrow interfaces if HEAD differs.

Current gaps matter for browser use: all run summaries returned without pagination/project association; create has no idempotency contract; resume reads and resets separately, so concurrent resumes can launch two executors; file writes after releasing the record lock can reorder snapshots and collide on one `.json.tmp`; persist failures are logged and swallowed while responses continue to imply successful acceptance; catalog snapshot is boot-only. Fix these with additive compatibility. Do not add a new general orchestrator or service credentials.

Read work/research-identity-note.md before choosing attribution/idempotency types. On the inspected edge-authenticated baseline, Sigil owns verified browser association while SDE stores caller-supplied metadata as unverified; unpredictable operation keys provide deduplication without pretending to grant authorization. Reconfirm the actual auth module after opening the workspace and report any materially different trusted-caller interface.

## Required behavior

- Add optional project and originator metadata to a run, backward compatible with existing saved records and callers. Treat supplied project IDs as bounded references, not authorization. Auth attribution must come from the verified gateway/caller contract, not a freely supplied identity string. If current SDE authentication cannot independently verify human identity, keep any external browser association explicitly unverified and let the trusted Sigil adapter own authoritative association; do not invent an actor claim from decoded unsigned JWTs. Resolve this boundary in a concrete interface note before coding identity behavior.
- Add bounded summary pagination with stable ordering and opaque cursor bound to filters. Existing no-query `/runs` remains a valid `{"runs": [...]}` response; additive pagination metadata is allowed. Avoid cloning full artifact records just to produce a summary page. Filter by validated project reference where available; legacy unassociated runs remain visible in a clearly separate global view.
- Implement optional idempotency for create at the service boundary, with bounded keys, validated caller scope and canonical request fingerprint. Concurrent identical requests return the same run ID and launch one executor; a reused key with different payload is 409. Persistence must retain the mapping across restart. If verified caller scoping cannot be added safely under existing edge auth, require a cryptographically unpredictable bounded operation key and document its exact semantics, without treating it as access authorization. No silent rerun after an ambiguous result.
- Serialize each run's state transitions and durable writes. Commit the accepted state before spawning new work or acknowledging a stage submission. Enforce resume eligibility and transition in one critical section, so only one resume starts. Stage submission must similarly check-and-mutate atomically against the expected awaiting stage/revision, including timeout races. Use unique temporary names and atomic rename with fsync where appropriate; do not let an older snapshot overwrite a newer one. Keep completed artifacts on restart/failure.
- Surface persistence failure honestly. Failed initial acceptance returns a typed service error and starts no executor. A failed stage checkpoint stops additional downstream inference, preserves the last durable record and exposes degraded durability/recovery status. If no durable store is configured, explicitly report memory-only mode, not durable readiness. Bounded health/read status should show persistence mode and failure without secret-bearing raw IO errors. Do not claim an in-memory error record will survive restart.
- Refresh the public catalog before a new run and resume with a bounded request/cache policy, validated known service descriptors, redirect policy and response size. Pin one resolved catalog snapshot for each execution attempt so a mid-run refresh cannot silently change dependencies. Record enough catalog version/observation metadata for inspection. Existing completed artifacts retain provenance. An unavailable refresh is reported as unavailable/stale under an explicit documented policy, never silently advertised fresh. Do not forward bearer tokens while fetching the public catalog.
- Preserve Setup 1: the same originator bearer is forwarded downstream and never persisted in run records, logs or responses. Expiry remains an honest stage failure with resume using a new caller token. No automatic refresh by SDE and no shared token fallback. Keep caller delegation defaults unchanged; the browser adapter will explicitly send `options.delegate=[]` when no attached caller agent exists.
- Keep existing handoff behavior and retroactive converge support. Correct outdated error/docs that imply a completed no-decision run can only be replaced when the supported caller-convergence route can unblock it. Handoff is a bundle for the driver; SDE never opens projects or writes to their Git repositories.
- Expose an additive, bounded live API-capability/version projection (for example alongside the existing `/healthz` version, preserving its liveness semantics) identifying the actual new run pagination/idempotency/durable-transition contract. The browser adapter must be able to distinguish this running binary from an older service that silently ignores new request fields. Source manifest declarations and an app's desired revision do not prove deployed API support. Document the exact feature response for B3b.

## Verification

Test first against temporary repositories/record directories and fake downstream HTTP services, without real models: two concurrent creates and resumes; repeated idempotency key with same/different body and after restart; lost/failed write and no downstream execution after rejected acceptance; ordered stage checkpoint races; submission versus timeout; legacy record decode; summary pagination/filter cursor validation and no artifact leak; catalog refresh failure and pinned execution snapshot; expired bearer failure and fresh-bearer resume preserving prior artifacts. Keep ordinary run/ADHD/handoff regressions green. Tests must actually assert effects, not mirror helper code.

Document exact additive API shapes for the Sigil adapter and changed recovery guarantees, with limitations. Run scoped red→green then relevant workspace regression/format/lint once, retain logs. Commit coherent changes and report full SHA, evidence, recovery rules, and any remaining release prerequisite. Parent performs independent review before session close/merge.

## Active execution context supplied by controller

Workspace already opened normally: SDE session `de4f0115`, branch `session/de4f0115`, exact task BASE `d06bfaaff8ff72085bb52ddd9a80c86447f3a5ba`. Do not open/recycle/close it. Parent read `/git/log?limit=15` before any other workspace request; no merge debt, not stale, clean status. Safe receipts: work/sde-implementation-open.json, work/sde-implementation-git-log.json, work/sde-implementation-status.json. Git handoff preserves recent attended research dossiers and existing delegation/retroactive convergence. Changed search since last close found no sde index; global mem0 fallback returned only the general awesome-ledger software digest, surfaced by controller. Current deployed driving contract is work/current-contract.txt.

Read actual repo AGENTS.md/CLAUDE.md and narrow service instructions if present through the workspace API, avoiding secrets/env. Do not change the Sigil repository: C2 is independently accepted separately at 18ad999df09dbc59296a1badd2d2f0cdbbef56e8. Scope is SDE source/tests/docs only plus your local report/evidence. Project code has no authoritative local checkout. Reads/snapshots may be local; source writes/builds/tests must be remote. Helpers accept project=sde.

Inspect existing file organization and use narrowly focused modules where this state-machine work needs them; no unrelated refactoring. Keep operation mapping and saved records bounded and internally consistent during restart and failed persistence. Authenticate provenance only through verified interfaces. Report the concrete auth/idempotency boundary before implementing it if it differs materially from the supplied identity note.

Capture outer workspace /exec exit, timed_out and truncated as well as actual background command exit and logs. An HTTP 200 or completed shell wrapper is not a passing test. Do not replay a long job after an ambiguous timeout; inspect its owned evidence/process identity first. Only stop task-owned fixture processes. Retain failed evidence. Use focused iteration; one relevant broad regression at the end, then rerun only code-covering checks after further fixes. Add the operational journal before the task commit. Leave session open and clean/pushed for independent review.
