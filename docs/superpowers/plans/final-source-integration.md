# Stage E source integration — bounded execution handoff

Execution brief after independently accepted D3. Accepted Sigil source is `5c1b5bfb3db893ec40e4e9206b55dbef8d356ca2` in session `877a5495` / branch `session/877a5495`. Accepted Memory source and E Memory task BASE is `64ea313d76d1e44e18050bfc9bc544a54f45eb6d` in session `516bb732` / branch `session/516bb732`. Parent publishes this brief and the updated plan through a normal Sigil documentation commit; its exact returned SHA will be supplied at dispatch as the E Sigil task BASE. Require clean exact HEADs before editing; do not start from stale session records. Read the accepted compact consumer notes and the relevant source/evidence only; do not reconstruct every prior task or rerun all component suites by default.

The user approved unattended implementation of the full operating environment. This source task completes controlled integration and prepares exact release inputs. A separate supported live activation step follows only when its actual host/edge/identity interfaces are available. There is no authorization to search for secrets, use host SSH/Docker workarounds, modify repository protection, or claim a live rollout from these fixtures.

## Ownership and constraints

Own the minimal Sigil controlled integration fixtures, necessary cross-component source corrections, final CI/check invocation and integration/release documentation. Memory ownership is limited to the actual curation/API producer contract, serializer/pagination, corresponding DTO/store helpers only where required by the response-budget join, focused test support/tests and contract/operational documentation (`memory/src/{api,curation,curation_tests,chunk,store}.rs`, narrowly associated modules and `docs/`). Preserve the enrollment authority, bounded storage, gate, authentication and cancellation guarantees. If a required correction lies outside this scope, identify the concrete dependency to the parent before expanding it. SDE, ADHD and Genie are read-only in this task; use accepted SDE fixtures/artifacts, and request parent context if an actual SDE source correction is necessary. Preserve all accepted implementations and other agents' changes; no subagents, concurrent implementation workers, session open/recycle/close, live data writes, paid inference, model downloads or deployment actions. Use only the authorized remote workspace API. Read `work/sigil-skill-redacted.md`; never print the original skill, private session JSON, environment values or tokens.

The global constraints in `outputs/sigil-os-implementation-plan.md` remain binding: preserve machine callers and original JWT pass-through; keep credentials out of public projections/logs; preserve failed work; dynamically enroll projects without rebuilding the frontend; keep human/agent sessions independent; preserve Memory provenance and curation across reindexing; separate source evidence from live verification; use Sigil's session branches and workspace API.

## Complete the remaining controlled joins

1. **Actual browser IDE controls.** Use `work/ide-final-integration-handoff.md` to reuse the existing pinned-provider/gateway and real companion fixtures. Join the browser toolbar to actual status/checkpoint/finish handlers using controlled runtime identity and temporary repositories. Observe save → checkpoint → independently read remote commit/blob → strict finish, including failure/recovery preservation. The old synthetic toolbar's unavailable response is not success evidence. Do not repeat the already accepted component status/handoff tests as a substitute for this join.
2. **Actual terminal, Git and extension interaction.** Exercise the pinned provider through the browser with a controlled terminal command and a real Git interaction. Exercise a supported extension path using a locally authored test extension/VSIX if appropriate, without downloading arbitrary extensions or contacting paid engines. Verify user-triggered work reaches the accepted activity/busy contract while passive polling, provider background traffic and a forgotten tab do not keep the session alive. Observe independent human and agent workspaces; browser disconnect must not merge or destroy work.
3. **Accepted project knowledge.** Reuse D3's real-handler fixtures to cover registration → overview → durable setup → verified namespace → accepted document bytes/provenance → actual Memory assets. Join a controlled SDE research attempt using fake engines to the accepted handoff and normal master-acceptance path where not already covered by D3. Read exact stored state, accepted SHA and imported bytes; do not invent success objects at the boundaries the test is meant to establish.
4. **Memory source editing and recovery.** Verify D3's authoritative owner/document association reaches D2 and C2's validated target launch, with indexed commit separate from current checkout and missing/moved/stale namespace cases explicit. Use actual assets for manual save, annotation and preview-confirmed forget against controlled records, with injected fake embedding only. Preserve durable save versus search projection and accepted commit versus indexed state.
5. **Mixed-version and interrupted flows.** Cover only remaining joins not already established by accepted tests: old running Memory/SDE capability gates, expired original caller context, ambiguous acceptance, failed push/merge, saved-but-unindexed material and stale confirmation. A GET/status/poll/browser disconnect must never start a mutation or mint a service credential. Keep uncertain work available for explicit authenticated recovery.
6. **Valid Memory response sizes.** Resolve the concrete producer/consumer budget risk in `work/memory-response-budget-handoff.md`. Valid full manual histories and accumulated/escaped content can exceed the current browser transport cap. Verify actual handler serialization through the adapter, then make the minimal bounded pagination/projection or route-specific correction necessary to keep supported content usable. This is a named integration concern, not a request for a general performance audit or a repeated full suite.

Source control fixtures can prove that the code enforces a policy response and uses distinct identities. Actual hosted-repository protection and a permitted merge identity remain separate live requirements. Do not weaken real repository rules or claim a local hook proves protection.

## Provider and regression qualifications

- Inspect `work/provider-compatibility-note.md` and the actual pinned package for the exact VSDA JS/WASM absence previously observed. Use primary package/maintainer evidence and supported configuration to determine whether this affects the required paths. Fix a supported integration error if identified; otherwise explicitly qualify the behavior. Never hide arbitrary browser errors or broaden console/abort exceptions beyond an observed, explained upstream resource.
- B3b accepted the measured native process-group candidate filter at `0d88aa2809452bd38c187173286057e664ea01b1`. The prior 100 ms observation budget exhaustion is resolved by that reviewed change with production budgets/custody intact. Read `work/timing-investigation-note.md` before investigating any remaining failure; do not restart that diagnosis or infer it explains every earlier failure.
- Historical full Sigil evidence remains 199 control-plane passes / 2 failures at two test threads, with an incomplete later suite tail. One deadline case reproduced on an accepted base at a different assertion; no accepted-base scheduler comparison existed. C1 minor N1 was an over-60-second diagnostic in a passing fixture. A new final run must be reported at its exact source revision without rewriting those observations.
- D3 also recorded a later crate-only run of 256 passes / 3 failures / 1 existing ignored provider test. Those failures asserted the old empty-registry behavior after registration; D3 changed the assertions to check immediate pending intent and preserved read-only state, then the affected registration/overview checks passed. This is a separate preserved non-green run, not a full-workspace result. Read the final independently accepted D3 report for subsequent fix-round changes/evidence before the settled E run.
- At this final integration stage, run the required Sigil workspace check once after the settled integration edits: `CARGO_BUILD_JOBS=2 cargo test --workspace -- --test-threads=2`, with complete durable logs and numeric completion. Inspect the existing CI invocation and align its documented concurrency if it still uses the known problematic default. Retain internal-concurrency tests. If the command fails before later packages/doc tests execute, identify and run only the missing required tail separately when appropriate; do not call the interrupted command a pass.
- Run the settled Sigil workspace formatter and existing Clippy invocation once as well: `cargo fmt --all -- --check` and `CARGO_BUILD_JOBS=2 cargo clippy --workspace --all-targets -- -D warnings -A clippy::too_many_arguments`. The sole allowance is the documented pre-existing one; do not add allowances to conceal new diagnostics. These final checks cover source amendments since the last accepted lint run. Any Memory harness/source amendment needs its affected release/offline tests and scoped formatter; unchanged tests are not repeated just for a new timestamp.
- Diagnose any remaining concrete failure through a bounded focused reproduction or source inspection. Preserve initial failures and exact fixture ownership. Do not loosen production deadlines, kill unrelated processes, allow either result without justification, or repeat a suite until a favorable run appears. Later tests must be justified by an amendment, failure or unresolved concern.
- Use the accepted Memory test-injection guard before any route can call model2vec/hf-hub. Do not invoke production models to qualify enrollment. D1's original possible model-lookup fixture caveat remains recorded; its final guard was directly tested. Unchanged SDE's 276-test/12-final-contract evidence and D1/D2 evidence are not to be rerun merely for a new timestamp.

## Evidence and operational output

Keep separate actual `.exit` and complete `.log` files, with first-class read receipts and exact source SHA. A job launch or successful outer transport is not completion. Inspect uncertain jobs rather than duplicating them. Preserve all failure evidence and owned fixture cleanup observations. Use synthetic data for screenshots, label it clearly and capture desktop/mobile only after settled assets; screenshots establish layout, not a deployed endpoint.

Write `work/final-source-integration-report.md` with exact interfaces exercised, real versus fixture boundaries, commands/outputs/actual exits, source fixes, per-repository commit/push/clean state, and any remaining live prerequisites. Provide a compact `work/final-source-integration-consumer-note.md` for the final reviewer/operator. Update operational logs and normal session commits; leave lifecycle to the parent.

Finish precise source-backed release inputs: final feature/configuration names and disabled defaults; required Memory/SDE contract versions and rollout ordering; browser versus machine route ownership; pinned provider/helper provenance and compatible images; storage writer/backup/rollback compatibility; ownership/enrollment migration and recovery. Reuse `work/rollout-brief.md`, `work/release-readiness.md`, `work/browser-origin-rollout-map.md` and `work/memory-backup-preparation.md`; do not invent a validated proxy configuration or a coherent backup that was not actually made.

Parent first applies the required independent E task gate to its exact BASE-to-HEAD changes, checking both spec and quality with covering evidence. Once that task is accepted, parent prepares complete immutable original-BASE-to-final-HEAD packages for one independent whole-branch review across changed repositories, using `work/final-integration-review-context.md`. This task worker does not dispatch a reviewer or begin a second implementation wave. Final review findings follow the applied SDD single-fix-worker/single-scoped-re-review rule. Supported live activation and normal accepted-source close are parent-owned subsequent steps.
