# Task D2 — the memory browser interface

Execution note: read driver instructions only from `work/sigil-skill-redacted.md`; never print the original credential-bearing skill or private session files. `work/sigil_client.py` handles authentication privately.

Depends on accepted B1/B2 and D1. D1 is independently accepted and merged to Memory master `4eefb57fcdf6d3a71744c16bdb58550b2f9c8a4c`; read `work/memory-curation-consumer-note.md` for the exact contract and `work/d1-fix1-evidence/docs__curation-api.md` for source documentation. The Memory workspace149fa31d is closed; D2 owns Sigil only and must not reopen Memory for source changes. Parent supplies exact API contracts and current Sigil session. Own the memory browser entry point, shared shell integration, typed memory adapter, UI tests and docs in Sigil. You are not alone in the codebase; preserve accepted work. No subagents, production memory writes/deletes, deployments or embedding downloads. Author code through the remote workspace API, commit normally, report `work/memory-ui-report.md`.

B2 integration notes (parent confirms accepted head at dispatch): shared routes use `/ui/`, including `/ui/projects/{project}/{tab}` and `/ui/memory`; bare project paths remain machine APIs. Assets are in `sigiledd/src/browser/assets/`; `SIGILED_BROWSER_DASHBOARD_ORIGIN` currently selects the exact dashboard site and other browser origins cannot serve those assets. Add the memory entry/asset origin deliberately rather than weakening all Host checks. Narrow JSON body allowlists are in `browser/dashboard.rs`. `window.Sigil` exposes `el`, `request`, `navigate`, `showAuth`, `checkSession`; preserve separate-tab sign-in/check and draft behavior when reusing the shell. `memory_adapter` is false until the actual adapter is implemented. The existing committed browser harness is `sigiledd/tests/browser-ui.cjs`, using actual authored assets and controlled fixtures; extend it coherently rather than creating a second incompatible shell.

## Experience

`https://memory.016180.xyz/` uses the common browser-originator login and serves the memory application. Route by an explicitly configured exact origin, never an arbitrary forwarded host. Its session cookie is host-only; Authentik supplies SSO with the Sigil host. Keep the existing memory machine API address/routes available through the deployment routing design. The memory app can be served by Sigil's browser module with a narrow server-side adapter; do not create a second identity system.

Use the shared visual tokens and a three-region layout: accessible index/project navigation, search/browse results, and selected memory/provenance details. On small screens use navigable panels with back/focus restoration. Show index/source counts only when observed, not guessed. Keep filters and selected record in the URL, with safe back/forward behavior.

Provide paginated browse and search, source/ref/path/tag/date filters supported by D1, clear empty/loading/stale/error states, and origin/index/revision/time information. Search results link to the exact detail route, not a full-index download. Display full provenance with safe source links. Imported text is read-only; expose annotations and source provenance instead of an Edit text button. Manual memories have create/edit/history, revision conflict handling, and save/index-projection states. Unknown metadata remains unknown.

For a source document with a verified registered-project association, provide “Edit source” through C2's authenticated file-target launch contract. Open the corresponding project-relative file in the human's IDE; never treat a memory's arbitrary ref/path as an executable URL or edit the source automatically. Show the indexed revision separately from the current checkout and a clear moved/missing/setup-required state when applicable. Sources outside an enrolled project retain their safe provenance link instead.

Keep an unsaved manual edit or annotation intact while polling/refreshing; a newer server revision is a visible conflict, never a silent replacement of the draft. Cancel/ignore slow responses for a previously selected index or memory. Verify these races and draft preservation in the browser checks.

An expired session must also preserve the current unsaved text and show sign-in/retry guidance. Do not automatically replay edits or forget actions after a new login; inspect current revision/result first and require the appropriate deliberate action.

Pin/unpin, archive/restore and annotation controls use the exact D1 target scope and optimistic revision contract. Explain whether an action affects a manual memory, a source document or a larger source scope. The user can inspect archived records and restore them. Preserve source/document overlays after reindex and do not treat pinning as a ranking claim.

Forget is a deliberate two-step UI: construct a nonempty scope, request a read-only preview, show the actual affected count and bounded sample, then send the exact server-issued confirmation/version after the user clicks the explicit action. Do not automatically retry a destructive request whose result is unknown. A stale preview requires a new preview and action; never expand scope invisibly. Explain that source files remain and suppression controls reindexing. Keep the protected mem0/index rules from the backend.

Show ingestion sources/history, pending/failed indexing and durable-save status. A manual memory saved durably but awaiting embedding must say so. Do not display a green saved-and-searchable state from a successful curation write alone. Reindex only known recorded sources through a deliberate action, not arbitrary user-supplied network/file URLs.

All adapters pass the actual browser access token, reuse B1's CSRF/exact-origin checks, and constrain fixed memory routes, request sizes and timeouts. Never forward cookies or arbitrary headers/URLs, emit tokens in JSON, or trust a browser-supplied actor. Render all memory/source content as text or strictly sanitized Markdown. Keep the UI free of SQL/store jargon unless the user opens a diagnostic detail.

Check D1's live capability/contract version before enabling new curation mutations, with bounded freshness and explicit unavailable/update-required states. A source declaration or desired app revision is not proof that the running service supports revision checks and suppression. Do not fall back from new forget/edit operations to legacy bulk deletion or content-derived insertion. Test the old deployed contract and verify that unsupported mutation controls cannot issue a write; compatible legacy search can remain available with an honest limitation.

## Verification

Use in-process adapter fixtures and actual authored browser assets with headless Edge/Playwright. Cover exact origin/login, pagination/filter/search, provenance, manual create/edit/409/history, imported annotation versus text editing, pin/archive/restore, save pending/failed projection, forget preview/changed-scope/stale/commit, safe malicious content and links, expired session, keyboard and mobile layout. Use only controlled temporary memory records; no live destructive or inference calls. Capture representative screenshots and report meaningful test evidence plus remaining live prerequisites. Update shared dashboard links so project memory capability opens the correct namespace without assuming ownership of a colliding index.

## Accepted IDE consumer reference

Cross-origin routing preparation is `work/browser-origin-rollout-map.md`. It separates the exact Memory browser root/assets from existing machine paths and records the host-only SSO requirements. Supply the actual new configuration names and final edge route examples in your report; that preparation map is not evidence of a live route.

C2 is independently accepted at 18ad999df09dbc59296a1badd2d2f0cdbbef56e8. Read work/ide-gateway-consumer-note.md for the exact authenticated file-target contract and decimal-string generations. Edit source requires D3-verified registered-project provenance, not an arbitrary metadata path; retain honest missing/moved/indexed-revision behavior.

D2 precedes D3 namespace enrollment. If the accepted D1 interface has no verified project/document owner claim yet, show source provenance safely and keep Edit source unavailable with a clear association/setup state. D3 must complete this join once its ownership/accepted-document contract exists; never infer verification from a matching index slug during D2. Preserve that explicit deferred join in your report and tests.

Version fidelity: accepted D1 manual/curation revisions are canonical decimal strings in 0..9223372036854775807; keep preview versions/confirmation exact. Never round a CAS value through JavaScript Number. Preserve accepted C2 full-u64 generation and B3 stage-revision behavior independently. Include 9007199254740993→9007199254740994 alongside ordinary stale-edit handling.

Use the backend's verified target kind to choose editing controls. A legacy chunk may have `source:"manual"` or a `memory:` ref while remaining a provenance document with a content-derived ID; neither string grants stable-manual editing. D1's authoritative stable identity, current projection and target are the contract. Preserve legacy document access and show imported annotation controls when the actual target is a document. POST/PUT stable-manual responses are bare Manual records while GET manual detail wraps `{memory,target,curation}`; the Manual record carries no index, so retain the selected route namespace separately. Confirm these exact shapes against the accepted consumer note at dispatch.

Use D1's accepted manual-create recovery identity for each new draft. Retain it across internal navigation and sign-in recovery, inspect that identity after an uncertain save, and reuse it only on deliberate retry. Never turn an ambiguous successful create into a second memory by inventing a new identity. A different actor must not take over the draft's authorized operation. Keep ordinary full-reload/closed-tab draft limitations explicit.

Accepted B3b lessons apply to the same browser boundary: scrub the current forwarded bearer from every final successful public projection as well as errors, including search text, metadata and cursor strings. Cover an actual adapter success that echoes its bearer and render that adapter output with the real assets; a standalone scrub-helper test is insufficient. Match D1's real UTF-8/blank/numeric validation before freezing a create snapshot. Distinguish a definite pre-reservation rejection from ambiguous acceptance so a corrected explicit submission can proceed without trapping the draft or discarding an uncertain identity. Preserve newer unsent edits and require deliberate action after login. Read work/research-adapter-consumer-note.md for the accepted shared research behavior to preserve while extending dashboard assets; avoid regressing its large revision strings and terminal handoff state.
