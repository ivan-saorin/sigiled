# Task D3 — automatic project memory enrollment and accepted research

Execution note: read driver instructions only from `work/sigil-skill-redacted.md`; never print the original credential-bearing skill or private session files. `work/sigil_client.py` handles authentication privately.

Follows A2/A3, B1/B2, B3, C and D1/D2. Parent supplies the accepted concrete interfaces. Own the minimal Sigil enrollment/operation integration plus a necessary additive memory namespace-ownership contract if D1 has not provided one. Ownership is explicit per repository at dispatch. You are not alone in the codebase; preserve others' changes. No subagents, live data migrations, deployment, external inference or unrelated namespace changes. Use the workspace API; report `work/memory-enrollment-report.md` and commit reviewed source separately in each repository.

## Required automatic behavior

- A newly registered project inherits desired IDE and memory capabilities from the accepted registry. Registration durably records setup work; bounded idempotent reconciliation creates/claims its project namespace and enrolls approved documentation from the canonical repository's accepted master revision. Existing project registration remains compatible, and failure after repo creation is visible as partial setup, never a lost or duplicated project. A new project appears in the dashboard immediately with truthful setup state.
- Namespace identity must be an explicit persisted project/repository owner claim, not just a matching slug. Existing unrelated indices, mem0 and intentionally shared indices are not auto-adopted or overwritten. Detect collisions and expose a corrective operation. Preserve existing membership/sharing choices. Retry after restart or service outage must not create a second namespace or replace ownership. Renaming/display-name edits do not move data implicitly.
- An authenticated Memory request does not by itself make its caller-supplied owner or accepted-commit fields verified. Before Sigil reports verified project association, enables Edit source, or claims accepted-document enrollment, independently bind the namespace claim and source identity to the actual registered canonical repository and accepted Git state. If Memory emits its own verified-attestation flag, it must derive that from a trusted validation path, not a public header or submitted label. Keep claim, verified association and access authorization distinct.
- Inspected memory `IndexMeta` has no per-human ACL or sharing field. The approved sharing requirement concerns explicit propagation into `mem0`, not proof of a new confidentiality boundary. Preserve the existing authenticated service access policy, expose namespace ownership separately from access authorization, and use wording such as “Project only” for default enrollment. Do not display “private to you” merely because the registry's desired sharing value is `private`; only claim enforced access restrictions if they are actually implemented and tested.
- Ingest only a bounded documented set of project documentation paths from the exact accepted source revision. Respect explicit manifest opt-out/path policies; reject traversal and unsafe sources. Use existing memory git/source ingestion contracts, adding a narrow supported route only if necessary. Keep provenance of project, source URL, ref/commit/path and operation. Do not clone whole workspaces including secrets or index every untracked file. Do not automatically index unaccepted research or scratch branches.
- Preserve a stable canonical document identity across revisions: source/repository ref/path identify the document, while the accepted commit/content SHA is separate provenance. Do not put a new commit into the identity on every enrollment and thereby bypass D1's annotations/suppression. Inspected `IngestRequest` has only source/ref/path/since/tags, so it does not by itself prove an exact-commit ingestion. Inspect the actual git-source implementation before selecting the transport. If memory cannot access a private repository with its existing authorized mounts, a narrow bounded document-ingestion route fed from Sigil's verified mirror is preferable to giving the memory service repository credentials. Reuse D1's durable projection/locking policy and keep imported content read-only in the UI.
- Research handoff becomes eligible for memory enrollment only after the driver confirms the accepted master commit containing the reviewed dossier and cover. Idempotency identity includes project, run and accepted commit; replay cannot create duplicate documents. If a session commit exists but master acceptance failed/has merge debt, report that state and wait. Do not mark indexing complete solely because the handoff bundle was generated or written to a session branch. Show pending/failed/ready operations in project memory and attention views.
- Keep enrollment operations durable, attributable, bounded and recoverable: state/revision, attempts, last safe error, next eligible retry, source identity and observed completion. Persist intent before calling memory and reconcile ambiguous responses using the deterministic idempotency identity. Keep raw credentials out of operation records/events. Separate desired capability from observed service success; expose unavailable/disabled/ownership conflict/awaiting authorization distinctly.
- Preserve accepted-revision ordering at the service boundary: a delayed original request or retry for an older accepted snapshot must not replace a newer accepted document projection. Exercise lost responses, overlapping accepted revisions and namespace recreation with the actual handlers. Idempotency alone must not silently authorize an out-of-order first acceptance; use a bounded, explicit version/ownership check where needed and preserve the last confirmed newer state.
- Confirm the running memory service advertises the accepted ownership/ingestion contract before attempting new enrollment writes. Extend D1's live capability projection when adding D3 routes. Unknown or older support produces update-required/pending state; never silently use legacy index creation in a way that loses namespace ownership or stable document/suppression semantics.

## Originator credentials are a binding constraint

Setup 1 says services do not acquire their own credentials. Enrollment is initiated under the registering/accepting driver's compatible JWT, or a currently authorized human browser context. Never substitute the bootstrap bearer, skill client secret, a service-owned refresh credential or a shared memory token. Forward the original access token only to the fixed trusted memory service. Keep it ephemeral for the bounded operation; never persist it in a queue payload.

Startup reconciliation can reconstruct queued intent without contacting memory under an invented identity. If no valid originator context is available, leave work in an explicit awaiting-authorization state until an authenticated mutation supplies one. Registration/acceptance can dispatch immediately with their real context; a Retry setup action can resume it after expiry. Safe GET overview/list calls remain read-only. An operator-triggered migration can enroll legacy projects in bounded batches under their real JWT. This is the deliberate recovery policy, not a claim that short-lived credentials allow perpetual background writes.

Accepted D1 at4eefb57 uses a fixed trusted Sigil verifier plus one exact configured `MEMORY_AUTH_ISSUER`. Enrollment can originate from the registering driver or a human browser context. Check their actual trusted issuer/subject contract; do not assume a dedicated human client necessarily shares the driver's issuer. If the supported configuration needs more than one trusted issuer, make the smallest explicit bounded allowlist/configuration extension with covering verified-identity tests and compatibility documentation. Never relax to arbitrary decoded issuers, substitute a service credential, or describe a rejected issuer as completed setup. Preserve the fixed verifier's signature/audience/service policy responsibility and stable issuer+subject attribution.

## Verification

Temporary repositories and fake/temporary memory services: new project registers once and appears before setup completes; namespace owner claim collision versus owned replay; restart recovery; failed/ambiguous network result; expired/no token stops writes; no token in persistence/logs; source allowlist/traversal; opt-out and later opt-in; accepted versus session-only handoff commits; duplicate acceptance; reindex preserving D1 curation/suppression; existing sharing unchanged; project deletion/recreation policy. Test full create→registry→setup→accepted handoff→memory metadata flow under controlled data with no paid inference or production mutation.

Finish the shared UI controls that earlier stages explicitly deferred. Produce exact release/migration instructions and test evidence. The integrated production workflow is only complete after Stage E live verification; do not claim a fake service test as a deployed namespace.

Explicit D2 join to finish: bind project namespace and imported-document provenance to the registered project owner claim, then enable the existing C2 Edit source flow only for verified project-relative sources. Verify the running ownership capability, not merely a matching index slug. Keep the indexed accepted commit separate from the current workspace checkout; missing/moved files remain explicit. D2 may deliberately leave this control unavailable until this contract is accepted.

Accepted B3b consumer contract is work/research-adapter-consumer-note.md at0d88aa2809452bd38c187173286057e664ea01b1, with Spec PASS/Quality Approved and all four findings/two joins closed in work/research-adapter-fix1-review.md. Read its durable operation/SessionRecord.handoff receipt and terminal-recovery contract before joining acceptance; D3 must preserve pending quarantine and the paused editor. Exact two-file helper DTOs and real controller/companion/native Git joins are already tested. Reuse those accepted boundaries; focus this task's tests on independently confirmed master acceptance and memory ownership/projection. Fixed service request limits must account for the exact serialized body, including escaping and metadata, before a definitely oversized operation can reach the remote service; do not confuse decoded content size with the downstream extractor limit.

## Browser consumer available for final dispatch

D2 is independently accepted at 3481d621b03a3952629436e3be9ad643216a3501 (original implementation a87bb8eba06099d8bc2f27bde9cfcd62730df898 plus test-only M1 fix). Spec Compliant/Quality Approved,0Critical/0Important/0Minor in work/memory-ui-review.md and work/memory-ui-fix1-review.md. Parent publishes this brief against that clean Sigil HEAD and supplies the resulting documentation commit as the exact task BASE. Exact consumer interface is work/memory-ui-consumer-note.md, with source/routing documentation in work/d2-evidence/source/docs/browser-memory.md. Memory UI exposes /browser/api/memory/indexes/{index}, keeps namespace separately from Manual, uses D1 verified targets and deliberately leaves project Memory links general/Edit source disabled. D3 finishes those specific shared UI joins. Preserve the explicit keyword/semantic/hybrid search action/cache boundary, uncertain draft/forget state, exact revisions and exact Memory origin configuration while integrating.

The approved design section198 specifies project documentation and the operational log as the default bounded enrollment source, source code only by explicit opt-in, and propagation to mem0 only by an explicit sharing choice. Reuse the accepted manifest policy rather than inventing a second set of defaults. Existing source/sharing settings and unaccepted work must be preserved.

## Exact workspace handoff for this task

Parent opened Memory session516bb732, branch session/516bb732, clean BASE4eefb57fcdf6d3a71744c16bdb58550b2f9c8a4c, no merge debt/staleness/image fallback. First workspace action was the15-entry Git handoff. Latest commits are accepted D1 curation and its reviewed lifecycle/legacy fixes; the earlier FD/optimization fix remains in place. Changed feed since the prior63eeeb9a close returned project-index404, then documented mem0 BM25 fallback200 with zero hits. No live inference or Memory write occurred. Receipts work/memory-d3-{open,git-log,initial-status,changes,changes-mem0}.json. Use the privately held session through sigil_client.py, never print private session files. Parent owns all close/recycle/acceptance; do not open another Memory or Sigil session.

Ownership: Sigil enrollment persistence/reconciliation, registration/normal-acceptance hooks, bounded machine/browser retries and status/attention projections, D2 namespace/source-edit joins, focused tests and docs. Memory ownership/accepted-document ingestion additions, curation/lifecycle/outbox participation and necessary bounded verified-issuer configuration, focused tests and docs. Preserve accepted source outside those joins and existing toolchains/auth contracts; no SDE/ADHD/Genie source changes. Parent supplies Sigil task BASE after publishing this brief against accepted3481d621.

Memory toolchain from D1: /usr/local/cargo/bin (invoke explicitly if PATH omits it), CARGO_HOME=/opt/cargo-home, CARGO_TARGET_DIR=/opt/memory-target, release builds with --offline. Prior installed matching rustfmt1.97.1 may not persist into the new disposable workspace; inspect before use. Routine matching formatter/tooling setup is authorized, but no model downloads or production embedder fallback in tests. D1 test harness directly rejects missing injected embedders before any model2vec/hf-hub loader. Use the accepted fake test path deliberately. Keep actual numeric completion files and first-class full read receipts; uncertain long jobs must be inspected, not duplicated.
