# SIGILED â€” Log operativo

**Contratto.** Questo file Ã¨ la memoria operativa del progetto. Ogni sessione che chiude lavoro coerente **aggiunge una voce in cima** (la piÃ¹ recente prima) e **aggiorna lo Stato attuale**. Ogni voce risponde a tre domande: **dove eravamo**, **dove prevedevamo di andare**, **cosa Ã¨ stato fatto** â€” piÃ¹ gli **scarti** fra previsione e fatto, lo **stato a fine sessione** e il **prossimo passo previsto**. I commit message sono la memoria fine; questo log Ã¨ la memoria grossa. Le voci non si cancellano: si correggono con voci nuove.

**Nota (SIGILED v2 DEC-04):** questo file Ã¨ project-owned â€” nasce dal template alla creazione e il template non lo tocca mai piÃ¹. Il layer macchina della storia (sessioni, merge, job) sarÃ  esposto da SIGILED via `GET /sigiled/projects/{p}/log`, senza scrivere nei repo.

---

## 2026-08-22 — memory registered: stack recall joins the catalog

- **Where we were:** catalog at 8 services (sde the latest); the `memory` project had just shipped M0–M4 (LanceDB+model2vec hybrid recall, isolated indexes + shared `mem0`) and deployed via `upgrade` — live at `memory.016180.xyz`, reddit corpus (23k chunks) ingested and answering hybrid queries in ~15 ms.
- **Done (driver session 2108c5ed, sigiled-claude):** `catalog.json`: `memory` added after sde — `https://memory.016180.xyz`, gate `stack-bearer`, spec `GET /search?q={query}&idx={a,b}&k={n}`, status `live`, skill `memory-recall` (the skill file lives in the memory repo, `docs/skill-memory-recall.md`: search before asking, cite ref+path). Committed only after the service was verified live through the edge. Full suite green in-session (110/110) — the embedded catalog parses and validates.
- **Note:** the served catalog is embedded at build (DEC-27) — `GET /services` publishes `memory` only after the control-plane rebuild/redeploy (operator: `./restart.sh`, as for sde).
- **Next:** operator redeploys the control plane; verify `GET /services?status=live` shows 9 services; memory-side next steps live in its own log (GIT_DEPLOY_KEY for private-repo ingests into mem0).

## 2026-08-16 â€” sde registered: the DEC-27 queued composed service is live in the catalog

- **Where we were:** catalog at 7 services; sde (the first consumer of the catalog, per the DEC-27 sequencing) had just finished its S5 â€” runs API, Setup 1 pass-through, `[app]` deploy.
- **Done, across three surfaces (driver session bfceda11 + the previous registration session + host-side):**
  - `catalog.json`: sde added â€” `https://sde.016180.xyz`, gate `stack-bearer`, spec `GET /openapi.json`, status `live` (committed only after the app was verified running; ff to master at `6066ab96`).
  - Edge: `sde.{$DOMAIN}` vhost appended to `/opt/stack/Caddyfile` on the adhd pattern (`import bearer` + authentik header scrub), validated in the caddy image before the write (backup `Caddyfile.bak-pre-sde-vhost`), hot-reloaded â€” first probe answered the TLS handshake and the gate's `401 missing bearer`.
  - Control plane: `./restart.sh` â€” rebuilt with the new embedded catalog; `GET /services?status=live` now publishes 7 live services, sde included.
- **Verified end to end through the public edge with a driver JWT:** `sde.016180.xyz/healthz` 200; its boot report resolves all five dependencies ready, `blocked_stages: []`; `POST /runs` without a bearer answers the Setup 1 envelope 401. sde had already proven pass-through live from a dev session (two six-stage runs, recorded in its own repo).
- **State:** catalog registration complete; sde is the first service whose deploy carried NO `[app.secrets]` â€” Setup 1 end to end.
- **Next:** nothing open on this side; sde's own next steps live in its PLAN (converge-shape probe, S6).

## 2026-08-15 â€” sessione 1fbfe9a8: DEC-28 ratificata, dispiegata e indurita

- **Dove eravamo:** DEC-28 semplificata (`any-authenticated` default) su master, produzione non toccata, ratifica in sospeso.
- **Cosa Ã¨ successo fuori dal repo, prima di questa sessione:** il Re ha ratificato in chat e il pattern Ã¨ andato sul bordo vivo â€” con tre lezioni pagate sul campo, ora documentate in `deploy/Caddyfile.example`: (1) il caddy dispiegato non lega `{args[N]}` negli snippet ("index is out of bounds" su ogni espansione) â†’ il nome del servizio ora deriva dall'HOSTNAME (`{http.request.host.labels.2}`), zero argomenti, call site invariati; (2) un bind mount di FILE singolo si stacca dall'inode quando l'editor salva via temp+rename â€” il container rilegge per sempre i byte vecchi; scrivere solo via redirect `cat >`, o montare la directory; (3) contare i domini nella riga di log "automatic TLS certificate management" â€” un config troncato in transito si carica senza lamentele. Verifica end-to-end a valle: JWT driver legge `genie`/`adhd` `openapi.json` e `search` JSON attraverso il gate (`granted_by: authenticated`); i chiamanti statici regrediti verdi; token spazzatura 401.
- **Fatto in questa sessione:**
  1. **Indurimento `/auth/verify`:** `X-Sigiled-Service` Ã¨ richiesto SOLO sotto `per-service` (dove Ã¨ input di policy); sotto il default Ã¨ informativo e la sua assenza non uccide piÃ¹ il braccio JWT â€” era esattamente il bug che il 15 sera ha trasformato un warning cosmetico del bordo in un braccio morto. Due test di trasporto a livello handler (mancante+default â†’ 200; mancante+per-service â†’ 500, presente â†’ 403). 110 test verdi.
  2. **`deploy/Caddyfile.example` allineato al dispiegato:** snippet `(bearer)` documentato, `(dual)` con gate inline a doppia credenziale e nome-da-hostname, le tre lezioni operative nel commento "Applying this to a live edge".
  3. **Registro Â§8: riga DEC-28 scritta** â€” proposta, emendata dall'operatore (per-callee â†’ any-authenticated default), ratificata e dispiegata lo stesso giorno.
- **Scoperto strada facendo (conta per sde):** `paper-api` non ha MAI avuto un token statico â€” il container porta solo `OIDC_ISSUER=â€¦/o/paper-api/` e `TRUST_EDGE_HEADER`; valida JWT del SUO issuer. Il `PAPER_TOKEN` in `/opt/sigiled/.env` Ã¨ un access token OIDC mintato una volta e ormai SCADUTO â€” per questo ogni sonda 401. Mint fresco dal provider `sigilled-paper` (client_credentials) â†’ autentica. E il catalogo mente di nuovo: `spec: GET /openapi.json` risponde 404 "no such endpoint" â€” paper serve una landing UI a `/` e nessuno spec endpoint. Conseguenze da sistemare (non in questo repo): `[app.secrets] PAPER_TOKEN` inietterebbe un token morto â€” servono client_id/secret e mint al volo, o la fiducia nell'edge header; e la riga `spec` di paper in `catalog.json` va corretta.
- **Stato:** control plane con l'indurimento su master (redeploy in coda, lo fa il driver via SSH subito dopo il close); bordo vivo giÃ  sul pattern DEC-28; registro e example allineati alla realtÃ .
- **Prossimo:** redeploy sigiledd + verifica; poi sessione su `sde` â€” S2: client genie (spec ora leggibile) e client paper (endpoint dalla sorgente del progetto `paper`, visto che lo spec endpoint non esiste).
## 2026-08-15 â€” sessione 3ec67e53: DEC-28 semplificata dall'operatore, e l'immagine di sessione riparata

**Dove eravamo.** DEC-28 proposta a fine sessione 5bb368aa con i grant **per-callee** (`svc:<servizio>` da `[compose]`) come regola, e il broker di token di sessione dichiarato prossimo passo obbligato.

**Dove prevedevamo di andare.** Ratifica o emendamento. Ãˆ arrivato l'emendamento, in chat: *Â«Basterebbe che il chiamante sia correttamente autenticato. Non mi serve la granularitÃ  per servizio.Â»* â€” con la richiesta di documentare comunque la fattibilitÃ  per-servizio come how-to.

**Cosa Ã¨ stato fatto.** `ServicePolicy` con due modi e default **`any-authenticated`**: il token Ã¨ comunque validato (firma RS256 contro la JWKS, `exp`, issuer sotto la base configurata) e ciÃ² che cade Ã¨ solo la domanda *quali* servizi quel chiamante puÃ² raggiungere. Il modo `per-service` resta implementato e testato ma **spento**, con `docs/per-service-authorization.md` come how-to: dichiara `[compose]`, provisiona i gruppi `svc:*`, gira una env var. Il bordo **non cambia** nel passaggio, perchÃ© il Caddyfile passa giÃ  il nome del servizio a `/auth/verify` e il modo default ne ignora il valore â€” scelta deliberata, cosÃ¬ l'eventuale stretta Ã¨ un flip di configurazione e non una migrazione.

Un valore di policy non riconosciuto **fa panicare il boot**, stessa dottrina di `catalog::assert_valid()`: una policy di sicurezza scritta male che ripiega in silenzio sul modo permissivo lascia l'operatore convinto di essere chiuso mentre Ã¨ aperto. Verificato dal vivo: exit 101 col messaggio che nomina i due valori validi.

108 test verdi, fmt e clippy puliti sui file toccati. Smoke: niente header di servizio 500, niente bearer 401, bearer forgiato 401, healthz 200.

**Scarti.** Due, entrambi costosi e istruttivi:

1. **Il fix al `Dockerfile.session` della sessione precedente era rotto.** `rustup-init` vuole `--component` **una volta per componente**: `--component rustfmt clippy` viene respinto con *"unexpected argument 'clippy' found"*. La build dell'immagine Ã¨ fallita e â€” esattamente come DEC-25 promette â€” la sessione Ã¨ caduta sull'immagine base globale **con lo shout in `build_error`**, cioÃ¨ senza cargo, senza cc, senza niente. Diagnosi immediata perchÃ© il messaggio di rustup arrivava verbatim nella risposta di `open`: leggere la risposta, non bisecare un Dockerfile. Corretto in `--component rustfmt --component clippy`, e siccome `[workspace]` si legge da **master**, Ã¨ costato commit + close + reopen prima di poter compilare una riga. Nessuna sessione puÃ² test-buildare il proprio dockerfile: niente docker nel container (regola 5). Riaperto su `vm-sigiled:df-06bf34aa4b47`, `build_error` assente, `cargo`/`fmt`/`clippy` nativi â€” la copia della rustup home in `$HOME` non serve piÃ¹ a nessuno.
2. **Il broker esce dal percorso critico.** Non era un ritardo, era un requisito della granularitÃ  per-progetto: `sigiled-claude` Ã¨ identitÃ  *driver* condivisa fra tutti i progetti e il suo JWT non dice *per quale progetto* sta agendo. Tolta la granularitÃ , il requisito si dissolve invece di essere risolto â€” il token del driver basta, che Ã¨ ciÃ² che SDE sperava fin dall'inizio. Resta come nota a piÃ¨ di pagina nell'how-to: Ã¨ il pezzo da costruire per primo *se* un domani si accende `per-service` e lo si vuole vincolante anche per le sessioni, non solo per app e job.

**Stato a fine sessione.** Master ha politica, arbitro, how-to e immagine di sessione funzionante. **La produzione continua a non cambiare**: `/auth/verify` Ã¨ additivo e nessun bordo lo chiama finchÃ© il Caddyfile vivo â€” sull'host, irraggiungibile da qualunque sessione â€” non viene aggiornato a mano. Registro Â§8 ancora **non toccato**: la riga DEC-28 la scrive la ratifica.

**Prossimo passo.** (1) Operatore: ratificare DEC-28 nella forma semplificata; (2) incollare lo snippet `(dual)` sul bordo col secondo argomento per ogni `import dual`, ricaricare, passare le tre curl in fondo al file; (3) **`sde` Ã¨ sbloccato da quel momento** â€” token driver accettato, specifiche di genie e paper finalmente leggibili, S2 si chiude su documenti e non su congetture.

---

## 2026-08-15 â€” sessione 5bb368aa: DEC-28 proposta, l'auth fra servizi composti

**Dove eravamo.** `sde`, primo servizio composto (sequenza DEC-27), si Ã¨ fermato a metÃ  S2: ogni servizio dello stack Ã¨ gated **al bordo**, quindi da dentro un container di sessione `genie/healthz`, `genie/docs`, `paper-api/openapi.json` e `adhd/openapi.json` rispondono **401** â€” e anche un token driver `sigiled-claude` appena mintato prende 401 su tutti e tre. **La specifica pubblica di un servizio Ã¨ illeggibile da dove si scrive il suo client.** SDE ha scelto di non scrivere due client indovinandone la forma, e lo ha messo per iscritto in `docs/composed-service-auth.md` del suo repo, con tre opzioni per il control plane. Questa sessione Ã¨ la risposta.

**Dove prevedevamo di andare.** Operatore in chat: opzione 1 (JWT scoped dall'IdP) **piÃ¹** opzione 3 (grafo di dipendenze dichiarato), chiedendo prima se le due confliggono.

**Cosa Ã¨ stato fatto.** Non confliggono â€” sono meccanismo e politica â€” ma leggendo *questo* repo sono emerse due correzioni al documento SDE, entrambe registrate in `docs/plans/2026-08-15-composed-service-auth.md` (**DEC-28, proposta, NON ratificata**):

1. **`aud=<callee>` non Ã¨ esprimibile.** `auth.rs` mette `validate_aud = false` di proposito (*aud Ã¨ il client_id del driver*), e in authentik l'audience Ã¨ impostazione **del provider**, non parametro per-richiesta di `client_credentials`: servirebbe un provider per ogni coppia *(chiamante, chiamato)*. Si usano i **gruppi**: `svc:<servizio>`, che Ã¨ ciÃ² che lo scope mapping `sigiled-groups` giÃ  emette e che `Claims` giÃ  parsa. Confronto a stringa intera â€” un match per prefisso farebbe passare `svc:genie-preview` per `genie` e allargherebbe in silenzio ogni concessione dello stack.
2. **Il bordo non deve imparare i JWT.** Caddy standard non li valida e servirebbe un build `xcaddy`, una seconda implementazione della crittografia e un secondo orologio contro cui sfasare `exp`. Inutile: sigiledd **giÃ ** valida RS256 contro la JWKS dell'issuer con `KeyStore` in cache, e il Caddyfile **giÃ ** usa `forward_auth` per la gamba umana. Il bordo chiede, sigiledd risponde.

Consegnato: `[compose] services = [...]` in `manifest.rs` (politica pura, validata solo nella forma â€” mai contro `catalog.json`, o il parse di un manifest dipenderebbe dalla data di build del control plane); `authorize_service_call()` + `GET|POST /auth/verify`; snippet `(dual)` a **doppia accettazione** in `deploy/Caddyfile.example` (bearer statico *oppure* JWT arbitrato, statico per primo cosÃ¬ la migrazione non rallenta chi non si Ã¨ ancora mosso). 105 test verdi, fmt e clippy puliti su tutto ciÃ² che Ã¨ stato toccato. Smoke contro un run locale: niente header di servizio 500, niente bearer 401, bearer spazzatura 401, healthz 200 â€” e quel 401 conta, perchÃ© senza IdP configurato l'estrattore `Actor` tratta tutto come dev admin e `/auth/verify` di proposito **non** eredita quella permissivitÃ .

**Scarti.** Quattro, tutti dal reale:

1. **Il broker non Ã¨ stato consegnato, ed Ã¨ una decisione.** SDE scriveva che le sessioni Â«cadono fuori gratisÂ» accettando il token del driver: **falso, e strutturalmente**. `sigiled-claude` Ã¨ identitÃ  *driver*, condivisa fra tutti i progetti; il suo JWT non porta nulla che dica *sto agendo per sde*. Quindi il bordo non puÃ² applicare una politica `[compose]` **per progetto** partendo da un token driver. O si accettano i token driver (e allora `[compose]` vincola solo job e app), o si minta un token **di sessione** che porti il progetto â€” l'opzione 2 di SDE. La seconda Ã¨ quella giusta ed Ã¨ il commit successivo naturale: vuole una chiave di firma per sigiledd e un braccio in piÃ¹ in `/auth/verify`. Non l'ho infilata a fine sessione: Ã¨ superficie nuova che *emette credenziali* nel control plane e merita la sua revisione.
2. **Conseguenza da dire chiaro: SDE non Ã¨ ancora sbloccato da questa sessione.** FinchÃ© non arriva il broker (o finchÃ© l'operatore non concede a mano i gruppi `svc:*` a un driver, che Ã¨ un allargamento vero e va scelto a tempo), una sessione continua a non poter leggere le specifiche di genie e paper.
3. **`cargo fmt` e `cargo clippy` non sono mai esistiti in una sessione di questo repo.** `Dockerfile.session` installava il toolchain `--profile minimal` senza componenti, e `RUSTUP_HOME` Ã¨ di root mentre l'agente Ã¨ uid 1000: i comandi non ci sono e non si possono installare. Recuperati in-sessione copiando la rustup home in `$HOME` (593 MB â€” via di recupero, non abitudine) e **bakati** nel Dockerfile per la prossima. Il riformat di tutto il repo che il silenzio aveva accumulato Ã¨ un commit a sÃ©; il primo clippy della storia di questo codice ha trovato due lint latenti (`catalog.rs:80`, `runtime.rs:301`) in file che non stavo toccando â€” **lasciati lÃ¬**, non aggiustati dentro un commit di feature.
4. **Un commit Ã¨ stato ricostruito.** Il primo `cargo fmt --all` ha travolto la feature con 24 file di riformattazione: `reset --hard HEAD~1`, riformat da solo, feature sopra, `push --force-with-lease` sul branch di sessione. La regola 2 dice che il commit Ã¨ la consegna, e una feature illeggibile sotto un riformat non consegna niente.

**Stato a fine sessione.** Master ha la politica e l'arbitro; **la produzione non cambia di una virgola**. `/auth/verify` Ã¨ additivo e nessun bordo lo chiama finchÃ© il Caddyfile vivo â€” che sta sull'host, irraggiungibile da qualunque sessione (regola 5) â€” non viene aggiornato a mano. Il registro Â§8 di `sigiled-v2.md` **non Ã¨ stato toccato**: la riga DEC-28 la scrive la ratifica, non la proposta.

**Prossimo passo.** (1) Operatore: ratificare o emendare DEC-28; (2) incollare lo snippet `(dual)` sul bordo, dare a ogni `import dual` il secondo argomento col nome del servizio, ricaricare e passare le due curl in fondo al file; (3) provisionare i gruppi `svc:*` estendendo `authentik-provision.sh` â€” dopo la ratifica, non prima; (4) il broker di token di sessione; (5) solo allora `sde` finisce S2 con le specifiche davvero lette.

---

## 2026-08-15 â€” sessione 211a4f42: gate DEC-27 allineati al bordo

**Dove eravamo.** Catalogo su master con i gate sondati com'erano: search `sso-only`, genie/adhd `edge-open`.

**Dove prevedevamo di andare.** Decisione operatore sui gate di genie/adhd; flip di search alla posa del metodo `(dual)`.

**Cosa Ã¨ stato fatto.** Deciso in chat (il Re, 2026-08-15): bearer al bordo per genie e adhd â€” opzione 1, una riga per vhost, nessun codice nelle app. `catalog.json`: search, genie, adhd â†’ `stack-bearer`. In coppia, consegnato all'operatore il Caddyfile del box aggiornato: snippet `(dual)` + search sul metodo + `import bearer` su genie/adhd (validato con caddy 2.10).

**Scarti.** Nessuno.

**Stato a fine sessione.** Catalogo e Caddyfile viaggiano insieme: incollare il Caddyfile e ricaricare il bordo prima o insieme al redeploy del control plane, cosÃ¬ `GET /services` nasce giÃ  vero.

**Prossimo passo.** (1) Operatore: Caddyfile sul box + reload; (2) redeploy sigiledd; (3) rigenera la skill; (4) il servizio composto, primo consumatore del catalogo.

---

## 2026-08-15 â€” sessione 877b0a9c: DEC-27, il catalogo dei servizi

**Dove eravamo.** Piattaforma completa post DEC-25/26; i servizi atomici dello stack (search, paper, folio, reddit, genie, adhd) esistevano solo nel Caddyfile del box â€” nessuna superficie che un LLM potesse interrogare per sapere cosa c'Ã¨.

**Dove prevedevamo di andare.** Il piano concordato in chat: catalogo prima, poi il servizio composto in coda.

**Cosa Ã¨ stato fatto.** `catalog.json` alla radice + `GET /services` (catalog.rs): embedded come il contratto, validato al boot (catalogo rotto = deploy fallito), pubblico. Schema per servizio: `name, purpose, machine{base,gate}, human?{base,gate}, spec, status, skill` â€” gamba machine obbligatoria. Skill template Â§3 (trio search/paper/folio + puntatore al catalogo). Contratto 2.2.0 (verbo + comando `services`). DEC-27 registrata e ratificata in chat: un solo metodo per i servizi dual â€” one vhost, bearer split â€” snippet `(dual)` normativo in `deploy/Caddyfile.example`. `cargo check` verde.

**Scarti.** Due, entrambi dal reale: (1) il piano diceva "file letto per request" ma il container deployato non ha il repo â€” embedded per costruzione, come il contratto; (2) i gate del seed sondati dal vivo: paper Ã¨ `service-token` (401 in-app), non edge-open; genie/adhd espongono `openapi.json`; search resta `sso-only` finchÃ© il suo edge non adotta lo split.

**Stato a fine sessione.** Master ha tutto; il canary serve ancora il contratto 2.1.0 â€” il catalogo va in onda al prossimo redeploy del control plane.

**Prossimo passo.** (1) Redeploy sigiledd (operatore); (2) rigenera la skill (`GET /skill/sigiled-claude`, approval-gated); (3) Caddyfile del box: `(dual)` su search, decisione bearer per genie/adhd; (4) il servizio composto, primo consumatore di `GET /services`.

---

## Stato attuale

_aggiornato: 2026-08-09, sessione b83823e4 (DEC-26 editto lingua + lettera alla prossima sessione: revisione anti-stale dei docs)_

- **DEC-26 â€” EDITTO LINGUA (2026-08-09)**: i file nei repo sono English-primary; chat e nomi possono essere italiani (es. `spina`); copia italiana `*_it.md` ammessa, mai in sostituzione; carve-out standing preservati: log-operativo in italiano by design, `git log` bilingue. Eleva a DEC il decreto della sessione 1b (README, commit 83e4466). Acconto anti-stale: riga di stato del v2 corretta (contratto 2.0.0 â†’ 2.1.0). Compito aperto per la prossima sessione: revisione anti-stale della documentazione â€” lettera nel log.


- **NOTA DI DESIGN Â«SIGILED COME OS PER AGENTI LLMÂ» (2026-08-09, NON ratificata)**: discussione in chat catturata in `docs/plans/2026-08-09-sigiled-as-agent-os.md` â€” mappatura dello stack su concetti OS (sessioni=processi, journal git=fs journaled, merge debt=fsck, elevate=sudo, searxng=NIC, folio=FPU, skill=binari, `GET /contract`=macchina autodescrittiva), modello a tre strati kernel/runtime/servizi (il runtime Ã¨ il microcodice per le errata del processore LLM), otto direzioni candidate con le posizioni del Re annotate. Nessuna DEC proposta.


- **IMMAGINI DI SESSIONE PER-PROGETTO (DEC-25, 2026-08-08)**: scoperto dal vivo che ogni workspace girava sulla vm-base globale â€” l'hook Â«FROM vm-base + layer progettoÂ» di DEC-17 non era mai stato implementato (una sessione su QUESTO repo Rust non aveva nÃ© cargo nÃ© cc). Ora: `[workspace] dockerfile = "â€¦"` in sigiled.toml â†’ sigiledd builda `vm-{p}:df-{blob12}` dal mirror a master (content-addressed sul blob del dockerfile: ribuilda solo a edit, alla open successiva, sincrona); sessioni in fallback **urlato** (`image: {used, requested, build_error}` in open/recycle), job **fail-fast**. Template: `[workspace]` attivo di default accanto al Dockerfile sottile. Questo repo: `Dockerfile.session` (rust 1.97.1 + build-essential) e manifest di radice rinominato `mgr.toml` â†’ `sigiled.toml` (la clausola DEC-22 Â«finchÃ© la v1 builda queste sessioniÂ» Ã¨ caduta al cutover). Contratto â†’ **2.1.0**. Piano della sessione: `docs/plans/2026-08-08-per-project-session-images.md`. Redeploy eseguito in sessione (ff di `/opt/sigiled` + supervisor restart, 75s, healthy) e provato dal vivo: build alla open, rotazione tag all'edit, `cargo check --workspace` verde DENTRO il workspace (22s) â€” trovata e riparata dal vivo la trappola `bash -lc`/`/etc/profile` che resetta il PATH (fix: `/etc/profile.d/rust.sh`, commit 930bb22; trappola documentata nel template). **DEC-25 ratificata dal Re in chat (2026-08-08).** Immagini `df-*` orfane prunate sul box.
- **FIX PIN-READER (2026-08-04)**: `template_version` era null per sempre su ogni progetto â€” `ProjectRecord::new()` parsava il pin ma nessun flusso di produzione lo chiamava con un manifest vero. Ora il tick dello scheduler (che giÃ  rilegge i manifest da master ogni ~5 min) rideriva i campi del pin via `Registry::refresh` â€” `needs_merge` mai toccato, persistenza solo a cambiamento. 90 test. Nota operatore: `template_behind` richiede `SIGILED_TEMPLATE_LATEST` nell'env del canary.

- **Su master**: docs (`sigiled-v2.md`, `v2-build-plan.md`, `sigiled-contract.md` 2.0.0-draft, questo log) + codice sessione 1: workspace cargo con `sigiledd/` (ex `seald/` â€” healthz, `GET /sigiled/contract`, `GET /sigiled/projects` con `template_version`; 7 unit test) e `vm-base/` (port del server v1, build-ext.sh su `ext-rust/`), `template/` (vm-tmpl v2), `images/build-vm-base.sh`, `tools/dev-toolchain.sh`, `CNAME` (`sigiled.dev`) + `index.html` (landing Pages).
- **RINOMINA COMPLETATA.** Per ordine diretto del Re (Â«eradica completamente la stringa sealÂ») il repo non contiene piÃ¹ alcuna occorrenza di Â«sealÂ» in nessuna forma: codice (crate `sigiledd`, env `SIGILED_BUILD_SHA`, header `x-sigiled-version`), documenti, contratto, template, commenti, **voci storiche di questo log comprese**. La storia vera (la piattaforma Ã¨ nata sotto due nomi precedenti, entrambi eradicati dal testo) Ã¨ preservata dalla storia git. Compilazione di scrupolo eseguita dopo lo sweep: `cargo build --workspace` verde (sigiledd + vm-base). Push del Re verificato: `ivan-saorin/sigiled` master = 2ab8f43, storia completa.
- **Flip-ready (sessione 1b)**: `LICENSE` **MIT** (DEC-23, ratificata dal Re 2026-08-02 â€” la proposta Apache-2.0 della 1b Ã¨ stata scartata: swap eseguito, un file + campo `license` nei crate); audit dell'intera storia git **pulito** (nessun bearer/token/chiave/IP: solo placeholder di spec); codice a **zero stack-specifics** (identitÃ  git di sessione con default neutri env-overridable, i docs restano liberi di citare lo stack esemplare); `template/Dockerfile` â†’ `FROM ghcr.io/ivan-saorin/vm-base:0.1.0` e `build-vm-base.sh` con lo stesso default (push esplicito `PUSH=1`); `docs/runbook-deploy.md` bozza; README riscritto EN-first; landing estesa (contratto, riconciliazione, security, self-host). Pubblicare = flip di visibilitÃ  + Pages + package Public.
- **ERADICAZIONE SIGLA V1 (DEC-22).** Per ordine del Re (Â«la sigla v1 non ha piÃ¹ senso, Ã¨ sigiled ovunqueÂ») il repo non contiene piÃ¹ la sigla dell'orchestratore v1 in nessuna forma: verbi a `/sigiled/*` (`GET /sigiled/contract`, `GET /sigiled/projects`), manifest v2 `sigiled.toml` (git mv nel template), design doc `docs/sigiled-v2.md` (git mv), provider `sigiled-*`, prosa e contratto (base `api.016180.xyz/sigiled`). Due eccezioni operative annotate: il manifest di **radice** conserva il nome file v1 finchÃ© l'orchestratore v1 costruisce le sessioni di questo repo (cade al cutover), e `mgr-smoke` resta lapide nel registry v1. La v1 in produzione risponde a `/mgr` fino al cutover â€” fuori repo.
- **Decisioni**: DEC-01â€¦DEC-22 registrate. DEC-11â€¦20 e DEC-22 ratificate, DEC-21 registrata (esito verifica naming: Â«sigiledÂ» pulito); DEC-01â€¦10 da ratificare â€” `sigiled-contract.md` resta draft finchÃ© non lo sono.
- **Progetti di piattaforma**: `sigiled` (codice + contratto â€” QUESTO repo, `ivan-saorin/sigiled`), il progetto v1 registrato col nome di battesimo (fondazione, da archiviare lato operatore su GitHub), `sigiled-supervisor` (resurrezione; nome confermato).
- **Sessioni SIGILED su `sigiled`: FUNZIONANO** (verificato 2026-08-02 sera, sessione ebdd0c8f â€” 201, container ok, build+test verdi). Il progetto era stato registrato per **adozione** (repo giÃ  esistente) e il temuto 503 sugli adottati **non si Ã¨ manifestato**: o il bug Ã¨ stato risolto, o non colpisce questo percorso. I progetti registry `seal`/`seal-supervisor` restano lapidi (SIGILED non ha delete): l'import al cutover le salta.
- **Per l'operatore**: (a) archiviare/eliminare i repo `-history` residui su GitHub se ancora presenti; (b) DNS `sigiled.dev` â†’ 4Ã—A GitHub Pages (185.199.108-111.153), `sigilled.dev` â†’ 301 HTTPS; (c) abilitare Pages su `ivan-saorin/sigiled` (source: master, root); (d) ~~build/push vm-base su ghcr~~ **fatto 2026-08-02**: `ghcr.io/ivan-saorin/vm-base:0.1.0` pushata dal box (resta da marcare Public il package); (e) token API Authentik in stack env per la sessione 3.
- **Sessione 2/4 COMPLETATA**: `GET /sigiled/projects/{p}/log` (JSON + `?format=md`) su store eventi in-memory; `template_behind` nel project record; hint `log_operativo_touched` come funzione pura (il verbo close che la userÃ  arriva con la sessione 4); `template/tools/sync-template.sh` + `template-allowlist` con drift detection su `TEMPLATE_VERSION`; e2e su `mgr-smoke` passato (drift indotto â†’ stop exit 2; sync pulito â†’ commit `805ca5c`). 15 unit test.
- **Sessione 3/4 COMPLETATA**: `auth.rs` â€” dual-auth Â§1.7 (bootstrap bearer constant-time = admin; JWT RS256 validato localmente via JWKS con cache, iss verificato prima di ogni fetch, gruppi â†’ ruolo, aud volutamente non pinnata: Ã¨ il client_id per-driver), capability map Â§1.6 come policy pura (approval per projects new, app verbs, sessioni piattaforma DEC-15), `POST /auth/elevate` device flow con custodia token nello store, `GET /auth/approvals`; rotte gated, healthz+contract pubblici. 28 test.
- **PROVISIONING IdP ESEGUITO (2026-08-02, token admin dal Re)**: provider `sigiled-device` (device_code) + `sigiled-claude`/`sigiled-kimi` (client_credentials) vivi con application, scope mapping `sigiled-groups`, gruppi `stack:*`; service account `ak-*-client_credentials` primati e in `stack:drivers`, `akadmin` in `stack:admins`. **E2E reale passato**: mint client_credentials sull'IdP â†’ JWT con `groups: ["stack:drivers"]` â†’ validato da sigiledd via JWKS live â†’ 200. Trappola documentata nel runbook: `grant_types` Ã¨ una allowlist â€” vuota = `invalid_grant` silenzioso senza eventi.
- **Sessione 4/4 COMPLETATA â€” BUILD PLAN FINITO**: `merge.rs` (close ff â†’ tre vie â†’ debt, pacchetto conforme al contratto, master intatto al conflitto) + `sessions.rs` (open senza lock con policy DEC-15 consumata e debt urlato, close sotto merge-lock per progetto, eventi nel log macchina; token/endpoint null fino al runtime container del cutover, repo da `SIGILED_REPOS_DIR`); test di concorrenza: due close simultanee serializzate (una ff, una merged), conflitto â†’ debt â†’ risoluzione che lo paga. 38 test. **Supervisor v0.1 su `sigiled-supervisor`** (147 righe, smoke verde, fix spawn_blocking). **Runbook completo** (deploy/rollback/recovery). Il contratto era giÃ  giusto: la v2 lo implementa, non lo insegue.
- **CUTOVER IN CORSO â€” fase 1 chiusa (2026-08-03)**: supervisor sul box (systemd, `User=auto`), **canary v2 vivo** (`sigiled-sigiled-1`, deploy via supervisor da `/opt/sigiled`, finestra dual-auth aperta col bearer storico), device flow attivo sul brand, licenza MIT (DEC-23). SSH driverâ†’box operativo; v1 individuata: sorgente `~auto/mgr` (Python), stato `mgr-data:/data/registry.json` + `keys/`. Fase 2.1 (persistenza) su master.
- **Fase 2.2 (runtime workspace) su master e VERIFICATA DAL VIVO (2026-08-03)**: `runtime.rs` â€” container `vm-{project}` su `mgr-net` (il nome Ãˆ il routing di Caddy, regola statica invariata), deploy key iniettata fra create e start e nel `core.sshCommand` del mirror (fix dal primo close live: il fetch non autenticava), mirror locale per progetto; open = mirrorâ†’containerâ†’healthâ†’clone+branch+push nel workspace; close = flushâ†’fetchâ†’merge localeâ†’push masterâ†’destroy. **Ciclo completo provato su `mgr-smoke`**: open dal canary â†’ workspace guidato ATTRAVERSO L'EDGE V1 (due header, regola statica: interop perfetta) â†’ commit `6515dd6` â†’ close v2: ff, push su GitHub, container distrutto â€” con la sessione sopravvissuta a un redeploy del control plane a metÃ  vita (la persistenza in azione). 47 test.
- **Fase 2.3 (import v1â†’v2) ESEGUITA (2026-08-03)**: `docker run â€¦ sigiledd import /v1` sul box â€” **10 progetti** importati (needs_merge conservato), lapidi `seal`/`seal-supervisor` saltate, **100 sessioni v1** nel log macchina (le altre: lapidi + `mgr-smoke` congelato dall'idempotenza, che Ã¨ anche il motivo degli 0 job run: erano tutti suoi), **9 chiavi** copiate + 1 giÃ  presente; un resume pendente segnalato: `oannes-one` branch `session/206f26ec` (da chiudere in v1 o riprendere post-switch). Verificato: `GET /projects` idrata i 10, il log macchina di `torchio` mostra le sue 5 sessioni v1 con epoch giusti.
- **Fase 2.4 (apps engine v2 puro) su master**: decreto del Re â€” *le app tutte v2 pure, niente v1 convertite; una migrazione per sessione*. `[app]` nel manifest del progetto (nome=DNS, niente `image:` per costruzione), `upgrade` = sha â†’ build `{nome}:{sha12}` in background (202+build record) â†’ recreate coi secrets risolti dallo stack env; `start` non ricrea mai; verbi gated dalla capability map. 58 test.
- **Sessione 5 (POST /projects) su master**: `github.rs` â€” il github.py della v1 portato in Rust e ristretto al verbo: generate da `vm-tmpl` (o **adozione** su 422 + probe del repo: un 422 di validazione vera resta un errore, niente progetti fantasma), deploy key ed25519 generata con `ssh-keygen` nel layout che `runtime.rs` giÃ  legge, chiave sul repo via API, registrazione per ultima (fallimento a metÃ  = stato pulito, verbo ritentabile). `GITHUB_PAT` nello `.env` del canary accende il verbo (assente = 503 onesto); `GITHUB_API_BASE` overridabile per i test (mock axum locale, zero rete). `Event::ProjectCreated` nel log macchina. 67 test. **Il rituale re-import Ã¨ morto**: i progetti nascono direttamente nel v2.
- **Sessione 6 (recycle) su master**: `POST /sessions/{id}/recycle` â€” flush best-effort col token custodito, destroy, recreate dal branch della sessione (`boot_workspace` resume=true) con token fresco; swap persistito nel record (il vecchio token muore lÃ¬), `Event::SessionRecycled` nel log macchina, branch-only path onesto per dev/test. 69 test.
- **Sessione 7 (reaper + resume) su master**: `reaper.rs` â€” loop tokio accanto al server (solo con runtime): `/health` di ogni sessione con token, `idle_secs` oltre soglia (env `SIGILED_REAPER_POLL_SECS`/`_IDLE_SECS`, default 60/3600) â†’ autosave flush col token custodito, destroy, record via, `Event::SessionReaped`; niente merge, il branch resta orfano su origin. Il resume Ã¨ il gemello: `find_orphan` in open() â€” branch `session/*` senza record vivo e non debitore â†’ ripreso `stale:true` con `last_commit`, che l'orfano venga dal reaper, da un crash o dalla v1. Zero stato nuovo nello store: il repo Ã¨ la veritÃ . Agent irraggiungibile = skip rumoroso, mai destroy su un hiccup. 72 test.
- **Sessione 8 (jobs engine) su master â€” IL SET DI VERBI v2 Ãˆ COMPLETO**: `[jobs.<name>]` nel manifest (cron 5 campi classici validati col crate `cron`, timeout 1..=60 default 30, `hc_ping` come riferimento env di stack, secrets come le app); run = container fresco `vm-job-{p}-{j}` (mai `vm-{p}`: sessioni e job coesistono senza lock) da master su branch `job-<name>-<stamp>` pushato subito, exec sotto wall clock, leftovers committati anche su failure, destroy, run record (running/succeeded/failed/timeout/error/skipped_locked/aborted-al-reboot), `Event::JobRun`, hc ping url|url/fail; scheduler con finestra (last, now] e refresh manifest ~5 min (`git show master:sigiled.toml` sul mirror); verbi `run`/`runs`/`GET /projects/{p}/branches`. Runtime generalizzato (container name esplicito + extra_env). 84 test.
- **ATTI FINALI ESEGUITI (2026-08-03) â€” LA V1 Ãˆ SPENTA.** Jobs verificati dal vivo su mgr-smoke (`[jobs.smoke]`: 404/202/409, run `succeeded`, testimonianza committata sul branch `job-smoke-*` e letta via recap, container `vm-job-mgr-smoke-smoke` distrutto a fine run; sessione 8b: l'esito del flush ora entra nel `detail` del run record â€” la prima verifica aveva scoperto un flush no-op silenzioso per colpa di `*.log` in `.gitignore`). Skill SIGIL riscritta v2-pura (zero riferimenti v1, canary `2bed307`). Sul box: `stack-mgr-1` fermato e rimosso dal compose, route `handle_path /mgr/*` rimossa dal Caddyfile (backup `*.bak-pre-v1-shutdown`; `mgr-net`, `mgr-data` e la route `@session /s/{project}` RESTANO â€” servono al v2), caddy ricaricato. Verificato: `/mgr/*` cade nel placeholder 502 by-design del vhost, `/sigiled/*` e `/s/*` vivi (ciclo openâ†’healthâ†’close completo post-spegnimento). `~auto/mgr` resta come archivio.
- **SESSIONE 1b COMPLETATA (2026-08-03)** â€” il repo Ã¨ **flip-ready**: LICENSE MIT alla radice (DEC-23; l'Apache-2.0 proposta dal piano era stata scartata dal Re â€” corretta in sessione), audit dell'intera storia **pulito** (68 commit, zero segreti), identitÃ  d'istanza **obbligatoria mai defaultata** (DOMAIN/GITHUB_OWNER, fixture su example-org), **`GET /skill/{driver}`** (DEC-24: la skill per-istanza si genera dal motore, client_secret vivo dall'IdP con AUTHENTIK_API_TOKEN, approval-gated), `docs/runbook-deploy.md` Â«contract vs referenceÂ» sui 5 sottosistemi + `deploy/` (.env.example, Caddyfile.example), **inglese lingua primaria** (sigiled-v2, v2-build-plan, authentik-setup EN ai path canonici; originali `*_it.md`; skill-migration solo `_it`, storica), README e landing aggiornati. 87 test verdi (scruple build sul box: il workspace v2 non porta cargo).
- **DEC-01â€¦10 RATIFICATE (2026-08-03)** â€” contratto **2.0.0**, testo giÃ  post-cutover (dual-auth chiusa, `/s/` token-only, search = superficie di stack). **Cutover auth ESEGUITO** (voce 8c72fb51): bearer storico 401, `/s/` token-only, entrambi i driver verificati, gamba umana provata da questa stessa sessione.
- **Prossimo passo previsto**: Dall'operatore: registrare la skill sigil e ritirare seal; package `vm-base` â†’ Public, Pages + DNS per il flip; client_secret dei driver nelle skill (chiusura finestra dual-auth). Rifiniture possibili: debt-resolution container che parte dal branch debitore, GET /sessions/{id} sul v2. (loop tokio su idle_secs, autosave, riapertura stale del branch â€” banco di prova: `oannes-one` session/206f26ec), poi jobs engine (`[jobs.*]` in sigiled.toml, scheduler cron, run su branch `job-*`). A feature verificate: purga v1 dalla skill sigil e spegnimento v1 (stop stack-mgr-1, via `/mgr/*` dal Caddyfile â€” `/s/{project}` resta). Dal Re: DEC-01â€¦10. Dall'operatore: package `vm-base` â†’ Public, Pages + DNS per il flip, client_secret dei driver nelle skill.

---

## Voci

### 2026-08-09 Â· Editto lingua (DEC-26) + lettera alla prossima sessione â€” driver: Kimi K3

- **Dove eravamo**: nota Â«SIGILED come OSÂ» salvata la stessa giornata (sessione 46a94339, ff su master); decreto lingua della sessione 1b (2026-08-03, commit 83e4466) vivo nel README ma mai elevato a DEC numerata.
- **Previsione**: il Re emette l'Editto in lettere cubitali â€” i file nei repo sono in inglese come lingua primaria; le chat possono essere in italiano; i nomi possono essere italiani (es. spina); una copia in italiano puÃ² esistere â€” piÃ¹ un compito da consegnare alla prossima sessione.
- **Fatto**: registrata **DEC-26** nella tavola di `docs/sigiled-v2.md` (riga di stato aggiornata con la ratifica; elevazione del decreto 2026-08-03 con carve-out standing preservati: `log-operativo.md` italiano by design come memoria di lavoro, `git log` bilingue); la nota lingua del README ora rimanda esplicitamente a DEC-26. Acconto sul compito anti-stale: la riga di stato del v2 citava contratto Â«2.0.0Â» â€” corretto a Â«2.1.0Â» (primo stale trovato e riparato).
- **Scarti**: nessuno sul merito. Trasparenza interpretativa: l'Editto Ã¨ stato codificato preservando i carve-out giÃ  ratificati (log-operativo italiano); se non conforme, il Re emenda â€” DEC-12 insegna.
- **Stato a fine sessione**: DEC-26 su master; il compito Ã¨ nella buca delle lettere (qui sotto).
- **Prossimo passo previsto** â€” LETTERA PER LA PROSSIMA SESSIONE (dal Re): revisionare la documentazione contro l'implementazione e l'architettura correnti per eliminare informazioni stale. Primo esemplare giÃ  catturato (versione contratto nella riga di stato del v2). Candidati: `docs/sigiled-v2.md`, `docs/sigiled-contract.md`, `docs/runbook-deploy.md`, `README.md`, `docs/skill-template.md`, `template/`, `docs/plans/`.

### 2026-08-09 Â· SIGILED come OS per agenti LLM â€” cattura discussione â€” driver: Kimi K3

- **Dove eravamo**: DEC-25 ratificata il giorno prima (immagini di sessione per-progetto); contratto 2.1.0; stack vivo.
- **Previsione**: nessuna â€” sessione nata da una domanda del Re in chat: Â«se sigilled Ã¨ un embrione di sistema operativo per agenti LLM, cosa dovrebbe avere out of the box?Â» (oggi: searxng, vm+github, folio).
- **Fatto**: discussione svolta e catturata in `docs/plans/2026-08-09-sigiled-as-agent-os.md` (inglese, da decreto lingua). Contenuto: (1) mappatura completa sigilledâ†’OS; (2) il test del kernel corretto su osservazione del Re â€” tre strati (kernel / runtime sempre linkato / servizi), criterio del runtime Â«la sessione lo ri-derivrebbe male da sola?Â», runtime come microcodice per le errata del processore LLM (confabulazioneâ†’search, aritmeticaâ†’folio, amnesiaâ†’journal); (3) otto direzioni candidate, ciascuna con la posizione del Re registrata in chat (journal come IPC asincrono, timer da studiare, Authentik vs credenziali esterne, accounting deprioritizzato, package manager Â«this is goodÂ», compute nodes in futuro, object store via $ref+hash, ricette meta).
- **Scarti**: due, entrambi guidati dal Re. (1) Scartata la mailbox/IPC live: le sessioni sono asincrone, basta un journal migliore â€” lettere, non telefonate; la sezione Â«open questionsÂ» del journal Ãˆ la mailbox, il rituale di open Ã¨ dove si legge la posta. (2) Scartato l'accounting da corporate (token/costi come sottosistema): OS personale â€” al massimo conteggi nelle voci di log, zero infrastruttura.
- **Stato a fine sessione**: nota su master, marcata esplicitamente NON ratificata; nessuna DEC proposta.
- **Prossimo passo previsto**: il Re decide se e cosa elevare a DEC; il candidato piÃ¹ economico Ã¨ la convenzione sui tipi di voce del journal (punto 4.1 della nota) â€” nessuna macchina nuova, una convenzione, e ci poggiano sopra IPC asincrono, coda batch e memoria delle adjudicazioni.

### 2026-08-08 Â· DEC-25: immagini di sessione per-progetto â€” l'hook di DEC-17 esiste davvero â€” driver: sigiled-claude, approval dell'operatore (sessione f53cbce3)

- **Dove eravamo**: sessione torchio del mattino (chat Â«ContinuiamoÂ») â€” confermato dal vivo che il container di sessione non ha nÃ© cargo nÃ© cc: TUTTE le sessioni giravano sulla vm-base globale (`SIGILED_VM_IMAGE`), il Dockerfile sottile che il template spedisce dal giorno di DEC-17 non era letto da nessuno. Handoff esplicito: sessione elevata su `sigiled` con piano proprio.
- **Previsione**: piano + implementazione dell'hook in un'unica sessione elevata.
- **Fatto**: piano in `docs/plans/2026-08-08-per-project-session-images.md`; DEC-25 registrata (EN+IT) â€” punto di pin `[workspace] dockerfile` nel manifest (mai convenzione sul nome file: il Dockerfile di radice di questo repo builda vm-base e NON Ã¨ un'immagine di sessione), tag `vm-{p}:df-{blob12}` content-addressed sul blob git a master, build sincrona alla open, sessioni in fallback urlato / job fail-fast. Codice: `manifest.rs` (`[workspace]` validato + `Manifest::from_repo`), `runtime.rs` (`session_image_tag` puro + `ensure_session_image` + `create_container` con immagine esplicita), `sessions.rs` (open/recycle risolvono in spawn_blocking, campo `image` nelle risposte), `jobs.rs` (stessa immagine, build rotta = run error). Template: `[workspace]` attivo accanto al Dockerfile sottile. Questo repo: `Dockerfile.session` (rust 1.97.1 + build-essential su vm-base) e rinomina `mgr.toml` â†’ `sigiled.toml` (clausola DEC-22 caduta al cutover). Contratto 2.0.0 â†’ **2.1.0** (campo `image`, Â§7 job, Â§8 chiave manifest).
- **Scarti**: nessuno di sostanza; la rinomina del manifest di radice non era nel piano del mattino ma Ã¨ il debito DEC-22 che scadeva proprio con questa feature (il lettore v2 probe `sigiled.toml` per primo).
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra.
- **Prossimo passo previsto**: operatore â€” redeploy sigiledd via supervisor; prova dal vivo: open su `sigiled` â†’ build di `Dockerfile.session` â†’ `which cargo` nel workspace; il Re ratifica DEC-25. Poi roadmap Â§8 passo 5 (ponte tomes-and-tales).

### 2026-08-04 Â· fix: il tick dello scheduler Ã¨ il lettore del pin â€” template_version non era mai popolato â€” driver: sigiled-claude, approval dell'operatore (sessione 2bc23b71)

- **Dove eravamo**: la sessione torchio (2026-08-04) osserva `template_version: null` su tutti i progetti anche dopo la finestra di refresh ~5 min; il sospetto era timing di deploy, la realtÃ  piÃ¹ netta: il lettore del pin non era collegato a nulla.
- **Fatto**: `ProjectRecord::new()` parsava il pin dalla sessione 1 (tre unit test lo coprono), ma nessun percorso di produzione lo chiamava con un manifest vero â€” le tre vie di creazione (POST /projects, import v1, jobs) fissano `template_version: None`, e il commento al sito di creazione prometteva un Â«flusso che lo leggeÂ» mai esistito. Ora quel flusso Ã¨ il tick dello scheduler, che giÃ  rilegge ogni manifest da master: `jobs_of` si sdoppia in `manifest_of` (manifest intero) + mappa jobs; `Registry::refresh(name, &manifest)` rideriva `template_version`/`template_behind` in place sotto write lock â€” `needs_merge` appartiene alla macchina dei merge e non viene mai toccato, quindi nessuna race con un close concorrente â€” e riporta se qualcosa Ã¨ mosso; il tick persiste una volta sola, solo a cambiamento. TDD rigoroso: red comportamentale visto sul test di tick (pin resta null) prima del wiring. 90 test verdi (3 nuovi: semantica refresh Ã—2, wiring a livello tick).
- **Scarti**: nessuno sul codice. Due note operatore restano dal rilievo torchio: (a) `template_behind` richiede `SIGILED_TEMPLATE_LATEST` nell'env del canary (assente = mai behind, by design); (b) verificare il tag reale di vm-tmpl citato dai pin v1.
- **Stato**: su master al close; il canary popola i pin entro ~5 min dal redeploy.
- **Prossimo passo previsto**: operatore â€” redeploy del canary con `SIGILED_TEMPLATE_LATEST` valorizzato; poi `projects` deve mostrare i pin veri su tutta la flotta.

### 2026-08-03 Â· fix: frontmatter YAML nella skill generata â€” driver: sigiled-claude, approval dell'operatore (sessione fafd0f9d)

- **Dove eravamo**: flip open-source eseguito dall'operatore e verificato (repo public, Pages+DNS vivi, ghcr anonimo, canary su master 1b); `GET /skill/{driver}` provato e2e col secret vero (primo token IdP scaduto â†’ 403 â†’ sostituito con API Token admin non-expiring).
- **Fatto**: il Re nota che le skill generate non hanno frontmatter â€” i loader di skill indicizzano su quello, senza non si registrano. `docs/skill-template.md` ora apre con frontmatter YAML (`name: sigil` stabile fra i driver, description di triggering con driver e istanza renderizzati); il test di render asserisce che il file generato inizi col frontmatter. 87 test verdi (scruple build sul box).
- **Stato**: su master al close; il canary serve il template nuovo dopo il redeploy dell'operatore.
- **Prossimo passo previsto**: operatore â€” redeploy canary, rigenerare le skill dei driver da `GET /skill/{driver}`.

### 2026-08-03 Â· sessione 1b â€” open source flip-ready, inglese primario, la skill si genera da sola â€” driver: sigiled-claude, approval dell'operatore (sessione 286d2d6b)

- **Dove eravamo**: cutover auth chiuso (contratto 2.0.0, bearer morto, v1 spenta); la 1b del build plan era l'ultima sessione mai eseguita. Il Re in chat: eseguirla, con tre aggiunte â€” un'API che genera la skill per-istanza dai valori custoditi, il runbook del setup del box, inglese lingua primaria coi doc italiani rinominati `*_it`.
- **Fatto**: (1) **LICENSE MIT** â€” prima commit erroneo Apache-2.0 seguendo il piano: DEC-23 aveva giÃ  deciso MIT; corretto in sessione. Lezione a commit: *il registro DEC batte le proposte del piano*. Audit storia: 68 commit, zero segreti (i 48-hex erano checksum di Cargo.lock, il PRIVATE KEY una fixture di test) â€” nessun rewrite. (2) **IdentitÃ  obbligatoria**: DOMAIN e GITHUB_OWNER perdono i default dell'istanza esemplare (expect al boot col subsystem attivo â€” un self-hoster smemorato fallisce forte, non opera in silenzio contro lo stack di riferimento); fixture di test neutre; authentik-provision.sh deriva basi e redirect da DOMAIN; DOCKER_GID variabile (988 era il gid di QUESTO box). (3) **`GET /skill/{driver}`** (DEC-24, richiesta del Re): render dal template embedded `docs/skill-template.md` coi valori d'istanza; client_secret vivo dall'API admin dell'IdP quando `AUTHENTIK_API_TOKEN` Ã¨ configurato, placeholder Â«copialo da AuthentikÂ» altrimenti; approval-gated come projects-new perchÃ© la risposta puÃ² portare una credenziale. (4) **Runbook** riscritto Â«contract vs reference implementationÂ» sui 5 sottosistemi (runtime, edge, IdP, GitHub, control plane) + `deploy/.env.example` + `deploy/Caddyfile.example` estratto dall'edge vivo, commenti preziosi compresi. (5) **Inglese primario**: design doc, build plan e authentik-setup EN ai path canonici, originali `*_it.md` ancora autorevoli sulla storia delle ratifiche; skill-migration solo rinominata (storica: la finestra che descrive si Ã¨ chiusa oggi); README (nota lingua riscritta, self-hosting reale) e landing (identitÃ  required, quickstart 3 comandi, verbo skill) aggiornati.
- **Scarti**: la licenza (sopra). Il naming era giÃ  chiuso (DEC-21): zero lavoro. Il workspace v2 non ha toolchain Rust (l'immagine Ã¨ runtime-only): build e test girati sul box in container `rust:1.97.1-slim` sul checkout del branch â€” pattern da tenere per le sessioni future su questo repo.
- **Stato**: 87 test verdi sull'ultimo sha. Repo flip-ready: pubblicare = flip di visibilitÃ  + Pages + package Public + DNS.
- **Prossimo passo previsto**: operatore â€” flip visibilitÃ  su `ivan-saorin/sigiled`, Pages (master/root), DNS `sigiled.dev` 4Ã—A GitHub Pages + 301 da `sigilled.dev`, package `vm-base` â†’ Public, `AUTHENTIK_API_TOKEN` nello stack env se si vuole la skill col secret vero. Il Re: conferma DEC-24 se la registrazione in chat non basta.

### 2026-08-03 Â· cutover auth â€” il bearer muore, la piattaforma riconosce i suoi driver â€” driver: sigiled-claude, approval dell'operatore (sessione 8c72fb51)

- **Dove eravamo**: contratto 2.0.0 ratificato con testo post-cutover; restava l'esecuzione operativa.
- **Fatto**: (1) credenziali driver lette dall'API Authentik (trappola: `AUTHENTIK_API_TOKEN` dello stack env Ã¨ scaduto â€” funziona `AUTHENTIK_BOOTSTRAP_TOKEN`); (2) mint `client_credentials` verificato per `sigiled-claude` E `sigiled-kimi` â€” trappola da contratto vivo: **`scope=sigiled-groups` obbligatorio** nel mint, senza il JWT esce senza claim groups e ogni verbo risponde 403; (3) edge `/s/` **token-only** (via `import bearer` dal blocco `@session`, backup `Caddyfile.bak-pre-token-only`); (4) `SIGILED_BOOTSTRAP_BEARER` rimosso dal canary (backup `.env.bak-pre-cutover`) â†’ bearer storico **401**, healthz/contract pubblici come da contratto; (5) verifica JWT completa: 200 sui verbi per entrambi i driver, ciclo openâ†’workspace(/s/ token-only)â†’close su mgr-smoke come actor `{driver: sigiled-claude, role: driver}`; (6) skill sigil aggiornata a client_credentials (bearer eradicato), credenziali kimi consegnate all'operatore fuori canale.
- **E QUESTA VOCE Ãˆ LA PROVA DELLA GAMBA UMANA**: elevate â†’ device flow â†’ approval dell'operatore (due codici scaduti prima di quello buono: la finestra di ~3 min Ã¨ stretta per un umano che non sta giÃ  sul browser â€” rifinitura possibile) â†’ sessione su progetto piattaforma con `actor.approval` valorizzato. DEC-01/02/03 complete e vissute.
- **Scarti**: `approvals.human` mostra `sigiled-device` (il nome del client) invece dell'umano â€” cosmetico, il decode dei claims va affinato; finestra device-code corta; da valutare rotazione stack-wide di `API_BEARER_KEY` (il valore ex-bearer vive ancora per superfici non-sigiled: search, files).
- **Stato a fine sessione**: SIGILED 2.0.0, un solo orchestratore, auth per-driver sola via macchina, approval umana viva. Il cerchio Ã¨ chiuso.
- **Prossimo passo previsto**: rifiniture a piacere dell'operatore (human claim, finestra device, rotazione API_BEARER_KEY, debt-container, GET /sessions/{id}).

### 2026-08-03 Â· ratifica DEC-01â€¦10 â€” il contratto esce dal draft: 2.0.0 â€” driver: Claude (sessione 1802ed6e)

- **Dove eravamo**: v1 spenta, piattaforma completa (85 test), contratto ancora `2.0.0-draft` in attesa delle dieci decisioni fondanti.
- **Fatto**: il Re ratifica DEC-01â€¦10 in chat (Â«ratifichiamo DEC-*Â», a piattaforma completa e verificata dal vivo â€” ogni DEC era giÃ  implementata: 01-03 auth/custodia/actor nelle sessioni 3 e 3-bis, 04-06 log/template/contratto nelle sessioni 1-2, 07-10 riconciliazione/debt/merge-commit/scrupolo nella 4). Contratto â†’ **2.0.0**: status ratificato, e il Â§1 descrive la realtÃ  POST-cutover che il Re ha ordinato contestualmente: finestra dual-auth CHIUSA (per-driver token unica credenziale macchina), workspace `/s/` token-only all'edge (il session token da 192 bit validato dall'agent Ãˆ l'auth; il gate statico era un residuo dell'era a bearer unico), search dichiarata superficie di stack fuori dall'auth SIGILED. Tabella DEC in `sigiled-v2.md` aggiornata (10 righe ratificate).
- **Scarti**: il cutover operativo (verifica driver sigiled-claude/kimi, edge `/s/`, rimozione `SIGILED_BOOTSTRAP_BEARER`) segue questa sessione â€” l'esito nella voce successiva.
- **Stato a fine sessione**: contratto 2.0.0 su master, da servire col prossimo deploy.
- **Prossimo passo previsto**: cutover auth e pensione del bearer.

### 2026-08-03 Â· atti finali â€” la v1 si spegne: sigiled Ã¨ l'unico orchestratore â€” driver: Claude (sessione 14f4d3a6)

- **Dove eravamo**: jobs engine su master, da verificare dal vivo; poi purga skill e spegnimento v1.
- **Fatto (verifica jobs)**: `[jobs.smoke]` dichiarato su mgr-smoke via sessione (cron notturno, run manuali). Via edge: 404 sul job fantasma, 202 col run record, 409 sul doppio run. Primo run `succeeded` ma recap VUOTO â€” indagine: `*.log` Ã¨ nel `.gitignore` di mgr-smoke, la testimonianza (`out/smoke-runs.log`) non veniva staged e il flush era un no-op pulito (sha del branch = master, la prova). Due rimedi: lo script scrive `.txt`, e â€” sessione 8b, TDD â€” `run_verdict` pura: un flush FALLITO finisce nel `detail` del run record, mai piÃ¹ silenzio (85 test). Secondo run: `succeeded`, testimonianza sul branch (`git show` via recap la legge, l'hostname dentro conferma `vm-job-mgr-smoke-smoke`), container distrutto, `job_run` nel log macchina.
- **Fatto (purga skill)**: `sigil/SKILL.md` riscritta v2-pura al canary `2bed307` â€” via la mappa di coesistenza, il rituale re-import, i fallback `/mgr`, il warning "niente reaper" (ora c'Ã¨, ed Ã¨ documentato con la semantica orfano/resume). Zero occorrenze v1.
- **Fatto (spegnimento)**: backup di compose e Caddyfile (`*.bak-pre-v1-shutdown`), `docker compose stop mgr`, servizio `mgr` rimosso dal compose (restano `mgr-net`, `mgr-data`), blocco `handle_path /mgr/*` rimosso dal Caddyfile (la route `@session /s/{project}` RESTA), `up -d --remove-orphans` (stack-mgr-1 rimosso), caddy reload. Verificato post-mortem: `/mgr/healthz` cade nel placeholder 502 by-design, `/sigiled/healthz` ok, ciclo sessione completo attraverso `/s/`. `~auto/mgr` Ã¨ archivio, l'immagine `mgr:0.6.1` resta sul box se mai servisse un'autopsia.
- **Scarti**: nessuno. Il flush silente era l'unico agguato, ed Ã¨ diventato un test.
- **Stato a fine sessione**: la piattaforma ha un solo orchestratore. Cinque app residenti v2, dodici progetti + `sigiled-smoke` nato dal v2, jobs/reaper/recycle/resume vivi e provati, 85 test.
- **Prossimo passo previsto**: vedi Â«Stato attualeÂ».

### 2026-08-03 Â· sessione 8 â€” jobs engine: l'ultimo organo, il build plan v2 chiude â€” driver: Claude (sessione c51750ca)

- **Dove eravamo**: reaper + resume verificati dal vivo su tre fronti â€” reap automatico a soglia corta su sigiled-smoke (autosave `wip: reaper idle 93s autosave`, container distrutto), resurrezione stale del branch mietuto (file recuperato), e il banco di prova storico: `oannes-one` session/206f26ec, il resume pendente lasciato dalla v1, ripreso dal v2 e chiuso ff a `69704fc9` â€” il lavoro di luglio (pipeline PDF issue-00) Ã¨ su master. Resurrezione cross-versione: la v1 ha mietuto, il v2 ha raccolto.
- **Previsione**: jobs engine come da build plan, test con job finto su mgr-smoke.
- **Fatto**: vedi Â«Stato attualeÂ». Scelte da ricordare: (1) container `vm-job-{p}-{j}` per non uccidere mai una sessione viva col `rm -f` preventivo (il runtime ora prende il nome container esplicito, con `extra_env` per i secrets); (2) manifest letto con `git show master:` â€” checkout-indipendente, "su master" letterale; (3) skip per latch = run record `skipped_locked`, non silenzio; (4) record `running` che sopravvive a un reboot diventa `aborted` all'idrata. TDD: 10 test rossi visti fallire, poi 84 verdi.
- **Scarti**: nessuno sul codice; la verifica live segue il deploy (voce prossima o aggiornamento di questa).
- **Stato a fine sessione**: master verde, tutti i verbi del contratto Â§4 implementati.
- **Prossimo passo previsto**: deploy + job finto su mgr-smoke via edge (run/runs/branches/recap), poi purga skill e spegnimento v1.

### 2026-08-03 Â· sessione 7 â€” reaper + resume: il contratto smette di mentire sulla regola 6 â€” driver: Claude (sessione 21c7f3fb)

- **Dove eravamo**: recycle verificato dal vivo (dopo il fix identitÃ  autosave trovato proprio da quella verifica: flush true, `wip: session recycle autosave` sul branch, file sopravvissuto, token vecchio 401).
- **Previsione**: loop tokio su idle_secs + resume del branch orfano; banco di prova reale il resume v1 pendente di `oannes-one`.
- **Fatto**: scoperta chiave â€” l'import 2.3 **segnalÃ²** il resume di oannes-one solo nel report, non nello store. Ne Ã¨ nato un design piÃ¹ pulito: niente flag `resumable`, la veritÃ  sta nel repo. Un branch `session/*` senza record vivo e non in coda debiti Ã¨ un orfano; open() lo riprende `stale:true` (`find_orphan`, entrambi i path). Il reaper diventa banale: flush autosave (col fix identitÃ  della 6b), destroy, record via, evento â€” il branch orfano Ã¨ il testimone. Copre in un colpo solo reaper, crash del control plane e lapidi v1. `reap_pass` non distrugge mai su agent irraggiungibile (hiccup â‰  idle). TDD: test rosso visto fallire, 72 verdi.
- **Scarti**: nessuno sul codice.
- **Stato a fine sessione**: master verde; verifica live (reap con soglia corta su sigiled-smoke + resume oannes-one) post-deploy, esito nella prossima voce.
- **Prossimo passo previsto**: deploy + verifica live reaper/resume, poi sessione 8: jobs engine.

### 2026-08-03 Â· sessione 6 â€” recycle: il passaggio di mano diventa un verbo â€” driver: Claude (sessione 3ace5bd7)

- **Dove eravamo**: POST /projects vivo e verificato dal vivo (progetto `sigiled-smoke` nato dal template, sessione aperta e chiusa sul neonato al primo colpo).
- **Previsione**: recycle come composizione â€” flush/destroy/create_container/wait_healthy/boot_workspace esistono tutti dalla fase 2.2.
- **Fatto**: `sessions::recycle` â€” 404 su sessione ignota, capability `Recycle` (nessuna approval: non Ã¨ territorio piattaforma-only), flush **best-effort** (un container incastrato Ã¨ metÃ  del motivo del verbo: si procede e si riporta `flushed` onesto), destroy, recreate dal branch con `boot_workspace(resume=true)` e token fresco, swap del token nel record persistito, `Event::SessionRecycled`. Il contratto Â§5 era giÃ  giusto: `{token, endpoint, sha_at_recycle}`. TDD: 2 test rossi visti fallire, poi verdi â€” 69 totali.
- **Scarti**: la prima verifica live ha trovato un bug **latente dalla 2.2**: `/exec` non ha l'identitÃ  git neutra che l'agent inietta solo sul suo `/git` surface â€” autosave su albero sporco morto con `Author identity unknown` (128), lavoro non committato perso col container. Mai morso prima perchÃ© i close live avevano sempre l'albero pulito. Fix (sessione 6844706a, TDD): `autosave_cmd` estratta pura e autosufficiente (`-c user.name/email` neutri, che perdono apposta contro l'env del container). 70 test.
- **Stato a fine sessione**: master verde col fix; ri-verifica live del recycle su albero sporco da fare post-deploy (l'esito va nella voce della sessione 7).
- **Prossimo passo previsto**: deploy + ri-verifica, poi sessione 7: reaper + resume.

### 2026-08-03 Â· sessione 5 â€” POST /projects: i progetti nascono nel v2, il re-import muore â€” driver: Claude (sessione 0a4ec01a)

- **Dove eravamo**: tutte e 5 le app v2 pure, edge route `/sigiled/*` viva, skill SIGIL compilata con i fallback v1 (fra cui `new` = verbo v1 + rituale re-import).
- **Previsione**: portare `POST /projects` sul v2 (logica da `github.py` v1), poi recycle, reaper+resume, jobs engine â€” una feature per sessione.
- **Fatto**: `github.rs` nuovo (client GitHub minimo: generate-from-template, probe di adozione, deploy key) + `project::create` (validazione nome = NAME_RE v1, 409 su registrato, 503 senza PAT, capability `ProjectsNew` giÃ  in auth.rs dalla sessione 3). Ordine v1 conservato: repo â†’ keypair â†’ chiave sul repo â†’ registrazione ultima. Miglioria sul v1: il 422 di generate diventa adozione **solo se il probe conferma che il repo esiste** â€” la v1 avrebbe adottato anche un errore di validazione. TDD rigoroso: 9 test rossi visti fallire (stub 501/false/Err), poi verdi â€” 67 totali, zero warning.
- **Scarti**: nessuno sul codice. Il deploy richiede `GITHUB_PAT` nello `.env` del canary (valore = `MGR_GITHUB_PAT` dallo stack env, azione operatore/driver al deploy).
- **Stato a fine sessione**: master verde, verbo pronto al deploy; verifica live (creazione reale via edge) da fare post-merge col canary ridesployato.
- **Prossimo passo previsto**: deploy + verifica live, poi sessione 6: recycle.

### 2026-08-03 Â· fase 2.2 â€” il runtime dei workspace: la v2 impara ad affittare container â€” driver: Claude Code (sessione 1359bf31)

- **Dove eravamo**: persistenza deployata e provata sul canary; runtime = l'ultimo organo mancante prima dell'import.
- **Previsione**: open/close che creano e distruggono workspace veri, col sorgente v1 come riferimento.
- **Fatto**: letto il sorgente v1 sul box via SSH (`sessions.py`, `docker_ops.py`, `workspace.py`, Caddyfile) â€” tre fatti che hanno disegnato il porting: (1) il routing Ã¨ **statico**: Caddy risolve `vm-{project}:8000` via docker DNS, quindi basta rispettare il nome e l'edge non cambia di una riga; (2) la deploy key viaggia col `docker cp` fra create e start â€” mai in immagini o volumi; (3) la v1 **merge-a via API GitHub** (solo ff): la v2 invece arbitra sul **mirror locale** (per questo sa fare le tre vie e il debt) e poi pubblica col push. `runtime.rs`: ensure_mirror (clone/fetch+reset), create_container (docker CLI â€” la v1 usa lo SDK python, shellare tiene ferma la lista dipendenze), wait_healthy, boot_workspace (clone+branch+push-early dentro il workspace), flush, push con la chiave del progetto; open/close di sessions.rs biforcano su `SIGILED_RUNTIME=docker` (assente = branch-only per dev/test: il workspace non ha docker per costruzione); push di master fallito = 409 col lavoro salvo sul branch. Immagine con docker-cli+openssh-client; compose con socket montato, `group_add: 988`, `mgr-net` esterna. Chiave di `mgr-smoke` copiata da `mgr-data` a `sigiled-data/keys` (anteprima dell'import: le chiavi v1 sono giÃ  nel formato che il runtime cerca). **47 test verdi, zero warning.**
- **Scarti**: (a) niente build dell'immagine progetto (la v1 la fa solo per gli ext vendored; i progetti v2 useranno il Dockerfile sottile â€” arriva con il recepimento del template al primo progetto reale); (b) recycle e resume del branch debitore rimandati alla rifinitura post-import; (c) reaper (auto-close a 1h idle) da portare â€” Ã¨ in v1 sessions.py, nota per la 2.4.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra. Master verde. Il primo open runtime dal vivo si fa post-merge (il supervisor deploya master).
- **Prossimo passo previsto**: deploy + open/close reale su `mgr-smoke` dal canary; poi 2.3 import.

### 2026-08-03 Â· cutover fase 1 chiusa + fase 2.1: canary vivo, persistenza su disco â€” driver: Claude Code (sessione cba506a5)

- **Dove eravamo**: build plan completo; il Re ha eseguito la parte operatore guidato in chat.
- **Fatto (operatore, guidato)**: supervisor deployato sul box (unit systemd con `User=auto` dopo un Â«dubious ownershipÂ» di git â€” il supervisor girava root su repo di auto; log chown-ato); `Dockerfile.sigiledd` + `docker-compose.yml` aggiunti al repo (il compose che il default del supervisor lancia; `.env` in `.gitignore`); **primo deploy reale via supervisor riuscito** (healthy:true, 63s, sha 7e90370) e verifiche canary passate (healthz, 401 nudo, 200 col bearer); `SIGILED_BOOTSTRAP_BEARER` allineato ad `API_BEARER_KEY` (il bearer che i driver giÃ  usano â€” il Re ne aveva coniato uno nuovo, la finestra dual-auth vuole quello storico); device flow attivato sul brand (flow `sigiled-device-code`, designation Stage Configuration â€” Authentik non ne ha uno di default) e sondato vivo. **SSH del driver sul box operativo** (chiave dedicata): trovato il sorgente v1 (`~auto/mgr`, Python) e lo stato (`mgr-data:/data/registry.json` â€” projects/sessions/job_runs + `keys/` per progetto): l'import ora si disegna sui dati veri.
- **Fatto (fase 2.1, questa sessione)**: `store.rs` â€” stato su file JSON atomico (tmp+rename, 0600) idratato al boot, stesso paradigma del registry v1; `SIGILED_STATE_DIR` (assente = effimero con warn; **corrotto = rifiuto di partire**: partire vuoti sovrascriverebbe â€” si restaura dal backup); custodia device-flow **persistita** (Â§1.4: SIGILED Ã¨ il custode) con API a soli metadati; `persist()` dopo ogni mutazione (open/close/elevate e task di polling); compose col volume `sigiled-data`. Test nuovo: conflitto â†’ debt â†’ reboot simulato â†’ coda debt e log macchina idratati. **43 test verdi, zero warning.**
- **Scarti**: la fsync del rename non Ã¨ forzata (il rename atomico basta per il crash del processo, non per il power loss â€” accettato, come la v1); il backup del volume `sigiled-data` va aggiunto a `backup.sh` dello stack (azione operatore, piccola).
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra. Master verde. Prossimo deploy del canary porterÃ  la persistenza.
- **Prossimo passo previsto**: fase 2.2 â€” runtime container (open/close che creano e distruggono i workspace veri, col sorgente v1 come riferimento sotto gli occhi); poi 2.3 import.

### 2026-08-02 Â· licenza MIT ratificata (DEC-23) â€” driver: Claude Code (sessione f73c9b24)

- **Dove eravamo**: build plan completo; la license Apache-2.0 della 1b era una proposta in attesa del Re.
- **Fatto**: il Re ratifica **MIT** in chat. Swap eseguito come promesso (Â«un file diverso, zero impattiÂ»): `LICENSE` = testo MIT canonico con copyright Ivan Saorin, `[MIT](LICENSE)` nel README, campo `license = "MIT"` in `sigiledd` e `vm-base`, DEC-23 registrata. Compilazione di scrupolo verde.
- **Scarti**: il repo `sigiled-supervisor` non ha ancora una LICENSE propria â€” da aggiungere (MIT, stessa DEC) alla prossima sessione su quel repo o prima del flip.
- **Prossimo passo previsto**: invariato â€” cutover Â§6; per l'operatore vedi runbook.

### 2026-08-02 Â· sessione 4/4 â€” concorrenza a riconciliazione, supervisor, runbook: il build plan chiude â€” driver: Claude Code (sessione f7f6e4ee)

- **Dove eravamo**: auth completa e provata sull'IdP reale; il Re dÃ  il via all'ultima sessione del piano.
- **Previsione**: i deliverable di Â§5 â€” niente lock di sessione, close con merge debt, supervisor, runbook, test di concorrenza su `mgr-smoke`.
- **Fatto**: (1) **`merge.rs`**: il motore del close â€” ff se master Ã¨ fermo, merge a tre vie (commit a due genitori, mai rebase) se Ã¨ mosso con cambi disgiunti, altrimenti **debt**: pacchetto identico al contratto (`branch`, `conflicted_files`, `ours`/`theirs` con sha e commit message, `since`), master intatto, branch superstite, `merge --abort` pulito. (2) **`sessions.rs`**: `POST /projects/{p}/sessions` senza lock â€” la policy della sessione 3 finalmente consumata (driver senza approval sui progetti piattaforma â†’ 403, con approval â†’ 201) e `merge_debt` in cima alla risposta; `POST /sessions/{id}/close` sotto **merge-lock per progetto** (la sezione critica ristretta ai secondi del merge, Â§4.1), hint onesto dal diff reale, eventi nel log macchina; la coda di debt esce anche da `GET /projects`. (3) **Test di concorrenza**: due close simultanee su lavoro disgiunto serializzano â€” una ff, l'altra vede master mosso e fonde; conflitto indotto â†’ debt col pacchetto completo â†’ risoluzione sul branch debitore che paga il debito al close successivo. 38 test verdi, zero warning. (4) **Supervisor v0.1** (sessione 13568118 su `sigiled-supervisor`, merged): un file, 147 righe, le tre API dei requisiti, rollback via sha, 409 su restart concorrente, log append-only; smoke run locale verde; bug reale scovato dallo smoke (comandi bloccanti sul worker tokio = deadlock del poll di health su 1 CPU) e fixato con `spawn_blocking`. (5) **Runbook completo**: deploy via supervisor con unit systemd, rollback, recovery a tre livelli fino a stack morto. (6) Refuso sweep nel contratto corretto (Â«confined to /workspaceÂ»).
- **Scarti**: due, strutturali e dichiarati. (a) Il test di concorrenza non Ã¨ girato su `mgr-smoke` via due sessioni MGR parallele: la v1 in produzione ha ancora il lock di monosessione â€” il 409 che la v2 elimina. Ãˆ girato sul motore vero con repo reali dentro la sessione: stessa logica che il runtime avvolgerÃ  al cutover. (b) `open`/`close` v2 gestiscono branch e debt ma non creano container (`token`/`endpoint` null): il runtime Ã¨ territorio del cutover, e il resume del branch debitore col merge in corso arriva lÃ¬.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra. Master verde. **Le 4 sessioni del build plan sono chiuse.**
- **Prossimo passo previsto**: cutover (Â§6 del piano, operatore+Re): canary, import registry (lapidi saltate), migrazione skill, switch edge.

### 2026-08-02 Â· sessione 3-bis â€” provisioning IdP eseguito, e2e machine-leg reale â€” driver: Claude Code (sessione afdaadec)

- **Dove eravamo**: sessione 3 chiusa con provisioning bloccato dal token outpost; il Re passa un token API **admin** (`akadmin`, verificato superuser).
- **Previsione**: girare `tools/authentik-provision.sh` e verificare la gamba machine sull'IdP vero.
- **Fatto**: (1) provisioning eseguito (da locale, con shim python3): scope mapping, gruppi, provider+app `sigiled-device`/`-claude`/`-kimi` (pk 2/3/4). (2) Il primo mint falliva `invalid_grant` **senza produrre eventi**: diagnosi a sorgente (token.py 2026.5.6) â€” tre cause reali, tutte corrette *nello script*: `redirect_uris` obbligatorio per schema; **`grant_types` Ã¨ una allowlist e vuota rifiuta tutto in silenzio** (ora esplicita per provider â€” Ã¨ il Â«grant ristrettoÂ» di Â§1.8 diventato campo); expression groups con `ak_groups` deprecato. (3) Priming: primo mint crea `ak-<driver>-client_credentials` â†’ messi in `stack:drivers`; `akadmin` in `stack:admins`. (4) L'e2e reale ha scovato un bug in `auth.rs`: `InvalidAudience` â€” l'`aud` dei token Ã¨ il client_id per-driver, la validazione aud va spenta (l'iss resta il cancello); fixato nei tre decode, 28 test verdi. (5) **E2E passato**: JWT mintato con client_credentials dall'IdP di stack, claim `groups: ["stack:drivers"]`, validato da sigiledd (run locale nel container) via JWKS live â†’ 200; bootstrap â†’ 200; senza auth â†’ 401. Runbook e script aggiornati con tutte le lezioni.
- **Scarti**: il provisioning Ã¨ girato dal PC dell'operatore-driver, non dal box (lo script resta pensato per il box: python3 non vive nei workspace); il token admin non Ã¨ stato persistito da nessuna parte â€” per la manutenzione futura va messo nello stack env (`AUTHENTIK_API_TOKEN`) dall'operatore.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra. Master verde. La gamba umana (device flow end-to-end con approval vera) si prova quando sigiledd gira sul box con i verbi della sessione 4.
- **Prossimo passo previsto**: sessione 4/4; operatore: client_secret dei driver nelle skill via UI, brand device-flow check, token admin in stack env.

### 2026-08-02 Â· sessione 3/4 â€” auth a due gambe: dual-auth, capability, device flow â€” driver: Claude Code (sessione 5f6f505d)

- **Dove eravamo**: sessione 2 chiusa; il Re passa in chat un token Authentik per il prerequisito Â§1 del piano.
- **Previsione**: middleware dual-auth, actor+capability, elevate device flow, provisioning provider via API, nota di migrazione skill.
- **Fatto**: (1) **Sonda del token**: Ã¨ quello dell'**outpost embedded** (`ak-outpost-â€¦`, non superuser) â€” liste filtrate a vuoto, create 403: provisioning automatico impossibile con questo token; il piano prevedeva il bivio e si Ã¨ preso il ramo Â«script + istruzioniÂ». Il token non ha toccato repo nÃ© log. (2) **`auth.rs`**: gamba legacy `SIGILED_BOOTSTRAP_BEARER` (constant-time, actor `bootstrap` admin) e gamba OIDC â€” JWT RS256 validato localmente contro JWKS con cache per kid, **iss verificato prima di qualsiasi fetch**, claim `groups` â†’ `stack:admins`/`stack:drivers`; capability map Â§1.6 come policy pura (`authorize`/`requires_approval`: approval per projects new, app verbs, sessioni sui progetti piattaforma DEC-15) â€” i consumatori sono i verbi della sessione 4, la matrice di acceptance del piano vive nei test. (3) **`POST /auth/elevate`**: device flow contro l'IdP, polling in background, token custoditi nello store e **mai serializzati fuori** (un test lo dimostra); `GET /auth/approvals` a soli metadati `{driver, human, expires, state}`, conformi al contratto. (4) Rotte esistenti gated (healthz+contract restano pubblici: il contratto Ã¨ il prodotto); auth non configurata = dev run con warn esplicito. (5) **Toolchain**: le prime dep C (ring) hanno scoperto due buchi di `dev-toolchain.sh` â€” mancava `cpp-14` (cc1) e `ar` senza la sua libbfd: fixati **nello script**, non solo nel container. (6) `tools/authentik-provision.sh` idempotente (gira sul box: usa python3) + `docs/authentik-setup.md` (runbook con fallback UI e wiring env) + `docs/skill-migration.md` (blocco da incollare nelle skill: mint client_credentials, retry-once su 401, elevate col codice in chat). Dipendenze nuove motivate: jsonwebtoken, reqwest(rustls). **28 test verdi, zero warning**, smoke run locale: 401 senza/con bearer errato, 200 col bootstrap, 503 elevate senza gamba OIDC.
- **Scarti**: (a) acceptance Â«open/close normali con JWTÂ» â€” i verbi open/close non esistono ancora in sigiledd (sessione 4): la matrice Ã¨ verificata a livello policy nei test, il cablaggio sui verbi Ã¨ il primo atto della sessione 4; (b) provisioning non eseguito per il permesso del token, non per mancanza di codice: con un token admin `tools/authentik-provision.sh` Ã¨ pronto; (c) introspezione come fallback di validazione rimandata (JWKS locale basta per la finestra).
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra. Master verde.
- **Prossimo passo previsto**: sessione 4/4 (concorrenza, supervisor, runbook completo); dal Re un token API Authentik **admin** per girare il provisioning.

### 2026-08-02 Â· sessione 2/4 â€” log macchina, template_behind, recepimento con drift detection â€” driver: Claude Code (sessione a8748f78)

- **Dove eravamo**: sessione 1b e eradicazione sigla v1 chiuse e merged; sigiledd skeleton (healthz, contract, projects con `template_version`); il Re dÃ  il via alla sessione 2 in chat.
- **Previsione**: i deliverable di Â§3 del piano â€” log macchina, hint di close, sync-template con drift detection, `template_behind`, banco di prova su `mgr-smoke`.
- **Fatto**: (1) **`events.rs`**: modello eventi taggato (`session_opened`/`session_closed`/`job_run`), store in-memory per progetto (persistenza al cutover, stesso patto del registry), **`GET /sigiled/projects/{p}/log`** JSON e `?format=md`, 404 su progetto ignoto, montato su entrambi i prefissi e verificato in run locale. (2) **Hint onesto**: `log_operativo_touched` come funzione pura sui path toccati dal branch (path esatto `docs/log-operativo.md`), pronta per il verbo close; l'evento `session_closed` la trasporta giÃ . (3) **`template_behind`** nel project record: pin vs ultima versione template pubblicata (`SIGILED_TEMPLATE_LATEST`), confronto semver numerico, unpinned mai behind. (4) **Recepimento**: `template/tools/sync-template.sh` â€” pin da `sigiled.toml` con fallback manifest v1 (DEC-22), sorgente `TEMPLATE_DIR` o clone shallow al tag, allowlist dichiarata **dal template** (`tools/template-allowlist`), drift detection su `TEMPLATE_VERSION` (tag + sha256 per file); mai auto-run, mai commit dallo script (DEC-07). (5) **E2E su `mgr-smoke`** (sessione a6e4646d, chiusa merged `805ca5c`): pin aggiunto top-level al suo manifest v1, primo sync committato, drift indotto â†’ **stop con elenco ed exit 2, zero scritture**, ripristino â†’ resync pulito ad albero immacolato. (6) template/README aggiornato (tools/ nel layout, ownership, `TEMPLATE_VERSION`). 15 unit test verdi; il contratto non Ã¨ stato toccato: **prometteva giÃ  tutto questo** â€” la sessione ha implementato il contratto, non l'inverso.
- **Scarti**: due, dichiarati. (a) Il piano dice Â«storia meccanica dal DBÂ»: il DB non esiste ancora â€” lo store Ã¨ in-memory dietro la stessa interfaccia, la persistenza arriva col cutover/import; l'endpoint e il render sono quelli del contratto. (b) L'hint di close vive come funzione pura + campo dell'evento: il verbo `close` di sigiledd che lo emetterÃ  Ã¨ territorio della sessione 4 (concorrenza) â€” cablarlo ora avrebbe anticipato mezzo close senza merge semantics. Primo appunto per la sessione 2 futura del template: il pin del banco vive nel manifest v1 (fallback), non in `sigiled.toml`.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra. Master verde.
- **Prossimo passo previsto**: sessione 3/4 (auth a due gambe) â€” serve il token API Authentik in stack env (prerequisito operatore Â§1 del piano), altrimenti la sessione produce istruzioni manuali.

### 2026-08-02 Â· eradicazione della sigla v1: SIGILED ovunque, DEC-22 â€” driver: Claude Code (sessione cbffcbd4)

- **Dove eravamo**: sessione 1b appena chiusa e merged; il Re, rivedendola, ratifica in chat il passo successivo: Â«la sigla v1 non ha piÃ¹ senso â€” Ã¨ sigiled ovunque, es. `GET /sigiled/contract`Â».
- **Previsione**: passata unica di eradicazione, analoga a quella del nome di battesimo.
- **Fatto**: sweep su tutti i file tracciati (21 modificati, 2 git mv): rotte di sigiledd `/sigiled/*` (router bare + nest, era il montaggio doppio di prima), `template/sigiled.toml`, `docs/sigiled-v2.md`, provider `sigiled-*` nei docs auth, base del contratto `api.016180.xyz/sigiled`, prosa Â«SIGILED v1/v2Â» al posto della sigla; registrata **DEC-22** (ratifica in chat); ripulite in corsa due frasi rese goffe dagli sweep sovrapposti (la nascita della piattaforma nel log, il Â«nato sigiledÂ» del piano). Compilazione di scrupolo verde (7 test). Nota di grafia: applicata **sigiled** (una L) da DEC-12; il messaggio del Re scriveva Â«sigilledÂ», letta come il refuso che `sigilled.dev` esiste apposta per assorbire.
- **Scarti**: due eccezioni deliberate, entrambe vincoli esterni e non scelte: il manifest di radice tiene il nome file v1 (l'orchestratore v1 cerca quel nome per costruire le sessioni di questo stesso repo â€” rinominarlo ora significava rompere la piattaforma sotto i propri piedi; cade al cutover) e `mgr-smoke` lapide nel registry v1 (nessun delete verb), annotato alla prima occorrenza nel piano.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra. Master verde.
- **Prossimo passo previsto**: invariato â€” sessione 2/4; al cutover: rinomina del manifest di radice e import del registry saltando le lapidi.

### 2026-08-02 Â· sessione 1b â€” open source flip-ready: license, audit storia, ghcr pubblico, README+landing, DEC-21 â€” driver: Claude Code (sessione b8e95f49)

- **Dove eravamo**: travaso a `sigiled` verificato funzionante (voce sotto); sessione 1b definita nel build plan Â§2b dopo la ratifica di DEC-19/20; `vm-base:0.1.0` giÃ  pushata su ghcr dall'operatore (package ancora privato).
- **Previsione**: i sei deliverable di Â§2b â€” license, verifica naming, audit storia, generalizzazione stack-specifics, FROM/script su ghcr, README EN + landing.
- **Fatto**: (1) `LICENSE` Apache-2.0, testo canonico da apache.org â€” **proposta**, ratifica del Re pendente. (2) **Audit storia git pulito**: scan dell'intera storia (17 commit) per bearer/token/chiavi/IP/password â€” zero valori reali, solo placeholder di spec (`<access-token>`, `$SESSION_TOKEN`); nessuna riscrittura necessaria; da qui in poi ogni commit si scrive sapendo che sarÃ  pubblico. (3) **Naming**: Â«sigiledÂ» verificato via web search â€” esiste solo come aggettivo (carte Magic, un campione di Raid, prosa Raku), nessun software/azienda/prodotto; registrata **DEC-21** (esito, non nuova decisione: il nome era giÃ  ratificato con DEC-12 emendata); riscritta la voce Â§2b del piano che dopo lo sweep attribuiva a Â«SIGILEDÂ» le collisioni del nome di battesimo. (4) **Generalizzazione**: inventario completo â†’ il codice aveva un solo stack-specific reale, l'identitÃ  git di sessione in `vm-base/git_api.rs` â€” ora default neutri `sigiled-session`/`session@sigiled.dev`, env-overridable (lo stack di riferimento inietta i propri alla creazione del container); descrizione `sigiledd` neutra; docs invariati per scelta (istanza esemplare). (5) **ghcr (DEC-20)**: `template/Dockerfile` â†’ `FROM ghcr.io/ivan-saorin/vm-base:0.1.0`; `build-vm-base.sh` con registry default ghcr e push esplicito `PUSH=1`; `docs/runbook-deploy.md` bozza (login/push, package â†’ Public passo-passo, bump di versione, note self-host). (6) **README EN-first** (mental model, contratto come prodotto, security in breve, nota lingua: docs interni in italiano per scelta) e **landing estesa** con le quattro sezioni del piano (contratto, riconciliazione vs lock, security model, self-host); HTML verificato bilanciato. Compilazione di scrupolo: `cargo build --workspace` + `cargo test` verdi (7 passed).
- **Scarti**: due, piccoli. (a) La voce naming del piano era giÃ  obsoleta: lo sweep di rinomina le aveva fatto dire il falso â€” corretta, e la verifica si Ã¨ ridotta a confermare che `sigiled` Ã¨ pulito. (b) La landing vive alla radice (`index.html`, dov'era giÃ  per DEC-19 con Pages source root), non in `docs/landing/` come scritto nel piano: si arricchisce sul posto, niente doppioni.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra. Master verde, repo flip-ready: pubblicare = flip visibilitÃ  + Pages/DNS + package Public.
- **Prossimo passo previsto**: sessione 2/4 (log macchina `GET /sigiled/projects/{p}/log`, sync-template, `template_behind`); ratifica Apache-2.0 dal Re; passi operatore nel runbook.

### 2026-08-02 Â· verifica del travaso: sessioni su `sigiled` funzionanti, adozione senza 503, ghcr fatto â€” driver: Claude Code (sessione ebdd0c8f)

- **Dove eravamo**: rinomina completata dall'altro driver (voce sotto); da questa parte risultava il progetto SIGILED del vecchio nome morto (404 GitHub, nessun redirect) e i nuovi `sigiled`/`sigiled-supervisor` giÃ  registrati per **adozione** â€” con il rischio teorico del bug 503 sulle sessioni degli adottati (DEC-14). Piano alternativo pronto (creazione fresca + import della storia), scartato perchÃ© il nome era ormai registrato.
- **Fatto**: (1) diagnosi del client GitHub di SIGILED: **non segue i redirect 301** (502 su repo rinominato) â€” l'operatore ha riallineato i nomi su GitHub e la sessione su `sigiled` Ã¨ partita: **201, il bug 503 sugli adottati non ha morso**. (2) Compilazione di scrupolo post-rename: toolchain bootstrap + `cargo build` + `cargo test` â†’ verdi (7 test, `sigiledd` + `vm-base`). (3) Registrato: `ghcr.io/ivan-saorin/vm-base:0.1.0` **buildata e pushata dal box dall'operatore** (PAT classic; package da marcare Public); utente box = `auto@automa`; la chiave SSH del PC operatore non Ã¨ autorizzata sul box (tentativo di delega fallito, istruzioni lasciate in chat).
- **Scarti**: il piano Â«rename in -history + creazione frescaÂ» concordato in chat Ã¨ stato superato dai fatti (adozione giÃ  avvenuta da un'altra chat) â€” Ã¨ bastato il rename inverso. Fortuna: l'adozione, che DEC-14 dava per rotta sulle sessioni, funziona; DEC-14 resta valida come dottrina per i progetti di piattaforma, ma il bug segnalato va ri-verificato prima di considerarlo un vincolo.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra. Master chiude verde.
- **Prossimo passo previsto**: sessione 1b.

### 2026-08-03 Â· eradicazione totale Â«sealÂ» â†’ Â«sigiledÂ», build verde â€” driver: Kimi K3 (sessione corrente)

- **Dove eravamo**: push del Re fatto e verificato (master = 2ab8f43 su `ivan-saorin/sigiled`); sweep di rinomina giÃ  avviato in questa sessione (git mv `sigiled-contract.md`, `seald/` â†’ `sigiledd/`, sed su Cargo e sorgenti Rust).
- **Fatto**: ordine diretto del Re â€” la stringa Â«sealÂ» va eradicata ovunque, senza eccezioni. Sweep case-preserving su tutti i file tracciati (10 file residui oltre a quelli giÃ  trattati): documenti, contratto, commenti del codice, landing, **voci storiche di questo log**. Riparati i punti resi insensati dallo sweep (nota del contratto, titolo README, sostantivo Â«the sigilÂ» nei commenti vm-base e nella regola 5). Compilazione di scrupolo: toolchain bootstrap + `cargo build --workspace` â†’ **verde** (`sigiledd v2.0.0-alpha.1`, `vm-base`).
- **Scarti**: (1) la strategia iniziale del driver preservava il wordplay Â«seal/sealedÂ» e i registri storici â€” ordine superiore ha prevalso, nessuna eccezione; (2) questo log perde consapevolmente la memoria testuale del nome precedente: la veritÃ  storica resta in git, il testo Ã¨ uniforme.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra.
- **Prossimo passo previsto**: progetto `sigiled-supervisor` (genesi) + archiviazione log del vecchio supervisor; rename della skill locale; poi sessione 1b.

### 2026-08-03 Â· il nome: SIGILED â€” rinomina, domini, landing Pages â€” driver: Kimi K3 (sessione d2e77904)

- **Dove eravamo**: DEC-12 (nome SIGILED) ratificata ieri; DEC-19 (open source + landing gh-pages) ratificata in serata; restava il problema del dominio pubblico. Nel frattempo Claude Code aveva chiuso la sessione 1/4 (codice!) â€” il peso di questo repo Ã¨ cambiato: non piÃ¹ solo docs.
- **Fatto**: esplorazione guidata dal Re con verifica RDAP sistematica (kunukku, editto, castellan, vsigiled, sigiledâ€¦); il Re ha scelto **sigiled** e acquistato la coppia `sigiled.dev` + `sigilled.dev` (il guardiano ortografico neutralizza l'ambiguitÃ  di spelling possedendo entrambe; `.ai` scartato: 83â‚¬/anno vs 13â‚¬/anno del `.dev`). Emendata **DEC-12**; sweep di rinomina su `sigiled-v2.md` (titolo, header, Â§7, DEC-15); aggiunti `CNAME` (`sigiled.dev`) e `index.html` (landing pubblica) alla radice. Progetto SIGILED `sigiled` creato. Il contratto/skill resta **SIGILED** â€” piattaforma SIGILED, contratto SIGILED; questione aperta se uniformare.
- **Scarti**: (1) il nome scelto non Ã¨ tra i quattro raccomandati dal driver (kunukku/editto/castellan/vsigiled) â€” il Re ha visto oltre; (2) la strategia di travaso Ã¨ cambiata in corsa: invece di partire da repo fresco, TUTTO il contenuto (codice sessione 1 compreso) viaggia con la storia â€” il push Ã¨ un comando solo dell'operatore, niente chirurgia sul DB di SIGILED.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra.
- **Prossimo passo previsto**: operatore pusha master di sigiled â†’ `ivan-saorin/sigiled` (force, primo push); poi sessione su `sigiled` per la **sessione 1b** (flip-ready open source, DEC-19/20) â€” che include l'abilitazione della Pages su `sigiled.dev`; DNS: 4Ã—A verso GitHub Pages su `sigiled.dev`, 301 HTTPS del guardiano `sigilled.dev`.

### 2026-08-02 Â· DEC-19/20 ratificate: open source al 100%, immagini pubbliche su ghcr; nasce la sessione 1b â€” driver: Claude Code (sessione b5245bba)

- **Dove eravamo**: sessione 1/4 chiusa in giornata; in chat, discussione su dove pubblicare le immagini base (prerequisito Â§1.3 del piano).
- **Fatto**: il Re ha ratificato in chat: (1) **DEC-19** â€” SIGILED v2 sarÃ  open source al 100% con landing GitHub Pages; il repo si scrive da subito come pubblico; (2) **DEC-20** â€” immagini `vm-base:x.y.z` pubbliche su `ghcr.io/ivan-saorin` (pull senza credenziali, PAT solo per il push, nessun segreto nelle immagini per costruzione). Scritta la **sessione 1b** nel build plan (Â§2b): license (proposta Apache-2.0), audit igiene della storia git, verifica naming (SIGILED collide con altri progetti), generalizzazione degli stack-specifics in config, FROM/script su ghcr, README EN + landing.
- **Scarti**: la decisione sul registry, lasciata all'operatore dalla sessione 1, si Ã¨ risolta in chat ed Ã¨ diventata piÃ¹ grande: non Â«quale registryÂ» ma Â«il progetto Ã¨ pubblicoÂ». Tentato l'accesso SSH al box dal PC dell'operatore per la build di vm-base: chiave non autorizzata (e host .205 con host key cambiata â€” segnalato); la build resta all'operatore con le istruzioni lasciate in chat.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ».
- **Prossimo passo previsto**: sessione 1b, poi sessione 2/4.

### 2026-08-02 Â· sessione 1/4 chiusa: fondamenta v2 su master â€” driver: Claude Code (sessione 03fe1b7d)

- **Dove eravamo**: solo docs su master; assessment appena registrato (voce sotto): greenfield per sigiledd, evoluzione per vm-base.
- **Previsione**: i deliverable di Â§2 del piano â€” layout, healthz+contract, parse del pin template, template v2, script immagine.
- **Fatto**: tutto Â§2. (1) Layout: workspace cargo, `server/`â†’`vm-base/` via git mv, `build-ext.sh` su convenzione `ext-rust/`, Dockerfile root a doppio ruolo (compat sessioni SIGILED v1 + sorgente di `vm-base:x.y.z`). (2) `docs/sigiled-contract.md` 2.0.0-draft generato da sigiled-v2.md, embedded in sigiledd a compile time (`GET /sigiled/contract` serve allo sha del build per costruzione). (3) sigiledd: `/healthz`, `/sigiled/contract`, `/sigiled/projects` con `template_version` dal pin `template = "vm-tmpl@x.y.z"` (parser con 7 unit test, incluso il test che il manifest del template non regredisca). (4) `template/`: Dockerfile sottile FROM vm-base:0.1.0, sigiled.toml commentato col pin, skeleton log-operativo, ext-rust/ vuota. (5) `images/build-vm-base.sh` (versione da vm-base/Cargo.toml). Acceptance verificata: build+test verdi, run locale su :8099 risponde su tutti e tre gli endpoint.
- **Scarti**: uno solo, strumentale â€” il container di sessione (runtime v1, debian-slim) non ha nÃ© rust nÃ© cc: per Â«build e test dentro la sessioneÂ» Ã¨ nato `tools/dev-toolchain.sh` (rustup + gcc-14/binutils/libc6-dev estratti da deb.debian.org in `$HOME/tc` senza root, ~2 min; effimero, da rilanciare dopo recycle). Da valutare in sessione 2 se l'immagine v2 debba includere la toolchain di serie.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra. Master chiude verde.
- **Prossimo passo previsto**: sessione 2/4 (log macchina + recepimento); per l'operatore, build+push di vm-base:0.1.0 e registry (Â§1.3), token Authentik in stack env per la sessione 3 (Â§1.2).

### 2026-08-02 Â· sessione 1/4, assessment iniziale: greenfield per sigiledd, evoluzione per vm-base â€” driver: Claude Code (sessione 03fe1b7d)

- **Dove eravamo**: piano in 4 sessioni su master; il prerequisito Â§1.1 (push del codice SIGILED v1 o decisione esplicita di greenfield) non era stato consumato.
- **Assessment** (il bivio del piano, risolto sull'evidenza del repo):
  1. Il codice dell'orchestratore SIGILED v1 **non Ã¨ su questo repo**: su master c'Ã¨ solo il vendoring vm-tmpl v1 (`server/`, `ext/`, `build-ext.sh`, `Dockerfile`, `sigiled.toml`) dell'Initial commit.
  2. `sigiledd/` nasce **greenfield guidato**: la spec del comportamento v1 Ã¨ il contratto SIGILED (la skill), la v2 Ã¨ `docs/sigiled-v2.md` â€” riscrivere in Rust da spec (DEC-16) Ã¨ piÃ¹ pulito che re-importare un codice che qui non esiste.
  3. `vm-base/` Ã¨ **evoluzione**: il server axum vendorizzato Ã¨ giÃ  Rust e giÃ  in produzione come agent dei workspace â€” si porta con `git mv` e diventa la sorgente dell'immagine `vm-base:x.y.z` (DEC-17).
  4. `template/` si riscrive sottile per costruzione (DEC-17/18): `FROM vm-base` + skeleton docs + `sigiled.toml` commentato + `ext-rust/` d'esempio.
  5. Vincolo pratico: il `Dockerfile` di root deve restare funzionante â€” SIGILED **v1** lo usa per costruire l'immagine di sessione di questo stesso repo â€” quindi si adatta a puntare a `vm-base/`, non si elimina.
- **Prossimo passo previsto**: layout repo, poi sigiledd (healthz + contract + parse template pin), template v2, script immagine. Voce di chiusura a fine sessione.

### 2026-08-02 Â· piano di costruzione v2 per Claude Code â€” driver: Kimi K3

- **Dove eravamo**: design completo (DEC-01â€¦18); il Re vuole costruire la v2 in Claude Code, in 3-4 sessioni.
- **Fatto**: scritto `docs/v2-build-plan.md` v0.1 â€” 4 sessioni (fondamenta+contratto+immagine base; log macchina+recepimento; auth due gambe; concorrenza+supervisor+runbook), prerequisiti operatore, acceptance per sessione, cutover. Il prompt per il driver punta al repo come memoria.
- **Scarti**: il piano prevede esplicitamente il bivio evoluzione/greenfield all'apertura della sessione 1 (il codice SIGILED attuale non Ã¨ noto al piano) e l'import del registry v1 al cutover.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra.
- **Prossimo passo previsto**: prerequisiti operatore (Â§1 del piano), poi sessione 1 in Claude Code.

### 2026-08-02 Â· DEC-17/18: workspace v2 = immagine base + ext per linguaggio â€” driver: Kimi K3

- **Dove eravamo**: questione aperta Â§6.7 (toolchain dei workspace); il Re: Â«per v2, immagine base + /ext-rust; i progetti python si costruiranno /ext-py, quelli go /ext-goÂ».
- **Fatto**: Â§3.1 nuova (immagine base pre-buildata per tag; `FROM vm-base:x.y.z` + layer progetto; ext-rust compiled-in, ext-py/ext-go come processi supervisionati proxati a `/x/<nome>`; vm-tmpl v2 con allowlist quasi a zero); Â§6.7 chiusa; DEC-17/18 ratificate. Nomi normalizzati a `ext-<lang>` col trattino.
- **Scarti**: nessuno sulla sostanza; normalizzazione formale dei nomi delle directory.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra.
- **Prossimo passo previsto**: costruzione v2 in 4 sessioni da parte di Claude Code, secondo `docs/v2-build-plan.md` (prerequisiti operatore Â§1 del piano); ratifica DEC-01â€¦10 in parallelo.

### 2026-08-02 Â· DEC-16: Rust per il control plane; questione toolchain â€” driver: Kimi K3

- **Dove eravamo**: v0.2 su master; in chat il Re decide il linguaggio di piattaforma.
- **Previsione**: registrare la decisione e verificare i fatti sul template.
- **Fatto**: letti `Dockerfile`, `build-ext.sh`, `sigiled.toml`, `server/` del vm-tmpl (via questo repo): l'agent `vm-base` Ã¨ giÃ  Rust/axum, `ext/` sono crate montati a `/x/<nome>` da build-ext.sh, il runtime Ã¨ debian-slim con git/bash/curl e basta. Registrata DEC-16 (Rust, ratificata); aggiunta questione aperta Â§6.7 â€” separazione template-owned/project-owned nel Dockerfile dei workspace (hook toolchain vs immagine base per tag).
- **Scarti**: la domanda del Re (Â«se uno vuole una vm in python?Â») ha rivelato che oggi ogni progetto **vendorizza e ricompila** l'agent â€” il recepimento di Â§3 Ã¨ ancora piÃ¹ necessario di quanto scritto in v0.1.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra.
- **Prossimo passo previsto**: ratifica DEC-01â€¦10; la questione toolchain si decide al primo progetto con toolchain non minimale.

### 2026-08-02 Â· v0.1 â†’ v0.2: autogestione ratificata, nasce sigiled-supervisor â€” driver: Kimi K3

- **Dove eravamo**: design v0.1 su master; questione aperta Â§6.2 â€” SIGILED SIGILED-registrato?
- **Previsione**: discussione in chat su implicazioni e limiti dell'autogestione.
- **Fatto**: il Re ha deciso: (1) la piattaforma v2 si chiama **SIGILED**, repo canonico questo; (2) la resurrezione Ã¨ un servizio â€” `sigiled-supervisor` (~100 righe, repo e deploy propri, API autonoma: chiamarla restarta sigiled); (3) bootstrap fresco + codice via prima sessione. Registrate come DEC-11â€¦15 ratificate; nuova Â§7 (autogestione) con tabella codice/servizio/stato/resurrezione; Â§6.2 chiusa. Progetto `sigiled-supervisor` creato e spec scritta nel suo repo.
- **Scarti**: il naming â€” il Re ha unificato contratto/progetto/codice sotto SIGILED, oltre la proposta di un repo separato per il solo orchestratore. E l'auth del supervisor: niente OIDC, deve funzionare a stack mezzo morto.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra.
- **Prossimo passo previsto**: ratifica DEC-01â€¦10; poi sequenza Â§5 passo 1.

### 2026-08-02 Â· nascita del repo + design SIGILED v2 v0.1 â€” driver: Kimi K3

- **Dove eravamo**: pattern nati nei progetti â€” torchio DEC-17/18 (recepimento, log operativo), tnt auth deployata (Authentik, gruppi, house key) â€” e lock di monosessione segnalato dal Re come scocciatura.
- **Previsione**: consolidare tutto in un documento di design della piattaforma.
- **Fatto**: progetto `sigiled` creato (repo `ivan-saorin/sigiled`); `docs/sigiled-v2.md` v0.1 â€” auth a due gambe, log operativo a due layer, template versioning, concorrenza a riconciliazione con merge debt e compilazione di scrupolo; DEC-01â€¦DEC-10; creato questo log (dogfood di DEC-04).
- **Scarti**: due, entrambi migliorativi e guidati da evidenza. (1) L'ipotesi iniziale Â«PAT mintati da console SIGILEDÂ» Ã¨ crollata dopo la prova dal vivo su `auth.016180.xyz`: discovery pubblica attiva, token endpoint e device endpoint vivi â€” Authentik Ãˆ giÃ  la console, i driver possono essere client OAuth2 di prima classe. (2) L'ipotesi del Re Â«device flow + refresh token nella skillÂ» Ã¨ stata analizzata e corretta nella forma: rotazione + concorrenza multi-chat la squalificano; la custodia passa lato SIGILED (DEC-02), mantenendo il device flow come gesto umano.
- **Stato a fine sessione**: vedi Â«Stato attualeÂ» sopra.
- **Prossimo passo previsto**: ratifica delle DEC-01â€¦10 da parte del Re; eventuale confronto incrociato con altro modello prima della ratifica.
